# Glen 自动化测试平台 - 项目重命名指南

> **原项目**: DCloud-Meter（滴云自动化测试平台）  
> **新项目**: Glen AutoTest Platform（Glen 自动化测试平台）  
> **操作日期**: 2026年1月11日

---

## 📋 重命名方案

### 新命名规范
- **项目名**: `glen-autotest`
- **Maven groupId**: `com.glen.autotest`
- **Maven artifactId**: `glen-autotest`
- **Java 包名**: `com.glen.autotest`
- **数据库前缀**: `glen_`

### 具体命名
- **后端模块**:
  - `glen-common`
  - `glen-gateway`
  - `glen-account`
  - `glen-engine`
  - `glen-data`
  
- **数据库**:
  - `glen_account`
  - `glen_api`
  - `glen_engine`
  - `glen_ui`
  - `glen_stress`
  - `glen_job`
  - `glen_dict`
  
- **前端项目**: `glen-autotest-frontend`

---

## ⚠️ 重要提醒

1. **备份项目**: 开始之前请先备份整个项目！
2. **顺序执行**: 必须按照以下顺序执行，否则可能导致项目无法运行
3. **测试验证**: 每个步骤完成后都要测试

---

## 🔄 重命名步骤

### 步骤 1：备份项目（必须）

```bash
# 备份整个项目到其他位置
xcopy E:\resume\yunautotest E:\resume\yunautotest_backup /E /I
```

---

### 步骤 2：修改 Maven 配置

#### 2.1 主 pom.xml
文件：`backend/dcloud-meter-master-.../pom.xml`

修改内容：
```xml
<!-- 修改前 -->
<groupId>net.xdclass</groupId>
<artifactId>dcloud-meter</artifactId>

<!-- 修改后 -->
<groupId>com.glen.autotest</groupId>
<artifactId>glen-autotest</artifactId>
```

#### 2.2 子模块 pom.xml（5 个模块）

**dcloud-common → glen-common**
- 文件：`backend/.../dcloud-common/pom.xml`
- 修改：
  ```xml
  <parent>
      <groupId>com.glen.autotest</groupId>
      <artifactId>glen-autotest</artifactId>
  </parent>
  <artifactId>glen-common</artifactId>
  ```

**dcloud-gateway → glen-gateway**
- 文件：`backend/.../dcloud-gateway/pom.xml`
- 修改同上，artifactId 改为 `glen-gateway`
- 修改依赖：
  ```xml
  <dependency>
      <groupId>com.glen.autotest</groupId>
      <artifactId>glen-common</artifactId>
  </dependency>
  ```

**dcloud-account → glen-account**
- 文件：`backend/.../dcloud-account/pom.xml`
- 修改同上，artifactId 改为 `glen-account`

**dcloud-engine → glen-engine**
- 文件：`backend/.../dcloud-engine/pom.xml`
- 修改同上，artifactId 改为 `glen-engine`

**dcloud-data → glen-data**
- 文件：`backend/.../dcloud-data/pom.xml`
- 修改同上，artifactId 改为 `glen-data`

---

### 步骤 3：重命名目录（手动操作）

在文件资源管理器中重命名以下目录：

1. `backend/dcloud-meter-master-...` → `backend/glen-autotest`
2. `backend/glen-autotest/dcloud-common` → `backend/glen-autotest/glen-common`
3. `backend/glen-autotest/dcloud-gateway` → `backend/glen-autotest/glen-gateway`
4. `backend/glen-autotest/dcloud-account` → `backend/glen-autotest/glen-account`
5. `backend/glen-autotest/dcloud-engine` → `backend/glen-autotest/glen-engine`
6. `backend/glen-autotest/dcloud-data` → `backend/glen-autotest/glen-data`
7. `frontend/xdclass-cloud-test-main` → `frontend/glen-autotest-frontend`

---

### 步骤 4：重构 Java 包名（在 IDEA 中操作）

⚠️ **这是最重要的步骤，必须在 IDEA 中使用重构功能！**

#### 操作步骤：

1. **打开 IDEA**，加载项目

2. **重新加载 Maven 项目**
   - 右键点击主 pom.xml
   - 选择 `Maven` → `Reload project`

3. **重构包名**（对每个模块执行）
   
   **glen-common 模块**:
   - 展开 `glen-common/src/main/java`
   - 右键点击 `net` 包
   - 选择 `Refactor` → `Rename`
   - 输入新名称：`com`
   - 勾选 `Search in comments and strings`
   - 勾选 `Search for text occurrences`
   - 点击 `Refactor`
   
   - 然后右键点击 `com/xdclass` 包
   - 选择 `Refactor` → `Rename`
   - 输入新名称：`glen`
   - 勾选所有选项
   - 点击 `Refactor`
   
   - 最后右键点击 `com/glen/xdclass` 包
   - 选择 `Refactor` → `Rename`
   - 输入新名称：`autotest`
   - 勾选所有选项
   - 点击 `Refactor`

4. **对其他模块重复上述操作**
   - glen-gateway
   - glen-account
   - glen-engine
   - glen-data

5. **验证重构结果**
   - 确保所有 import 语句都已更新
   - 确保 @MapperScan 等注解中的包名已更新

---

### 步骤 5：修改 Mapper XML 中的 namespace

在 IDEA 中全局搜索替换：
- 按 `Ctrl+Shift+R` 打开全局替换
- 查找：`net.xdclass`
- 替换为：`com.glen.autotest`
- 范围：`*.xml` 文件
- 点击 `Replace All`

---

### 步骤 6：重命名数据库

#### 方案 A：创建新数据库（推荐）

在 MySQL 中执行：

```sql
-- 创建新数据库
CREATE DATABASE IF NOT EXISTS glen_account DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_api DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_engine DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_ui DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_stress DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_job DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_dict DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS nacos_config DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

-- 然后使用 Navicat 的数据传输功能，将 test_* 数据库的数据复制到 glen_* 数据库
```

#### 方案 B：重命名现有数据库

```sql
-- MySQL 不支持直接重命名数据库，需要通过 mysqldump 导出再导入
-- 或者使用 Navicat 的数据库复制功能
```

---

### 步骤 7：修改 application.properties

修改所有模块的 `application.properties` 文件：

**glen-gateway**:
```properties
# 修改数据库连接
spring.datasource.url=jdbc:mysql://localhost:3306/glen_account?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
```

**glen-account**:
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/glen_account?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
```

**glen-engine**:
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/glen_engine?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
```

**glen-data**:
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/glen_api?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
```

---

### 步骤 8：修改前端配置

修改 `frontend/glen-autotest-frontend/package.json`：

```json
{
  "name": "glen-autotest-frontend",
  "version": "1.0.0",
  ...
}
```

---

### 步骤 9：清理原作者注释

在 IDEA 中全局搜索替换：
- 按 `Ctrl+Shift+R` 打开全局替换
- 查找：`小滴课堂,愿景：让技术不再难学`
- 替换为：`Glen AutoTest Platform`
- 点击 `Replace All`

- 查找：`@Author 二当家小D`
- 替换为：`@Author Glen Lee`
- 点击 `Replace All`

- 查找：`@Remark 有问题直接联系我，源码-笔记-技术交流群`
- 替换为：`@Remark Glen AutoTest Platform`
- 点击 `Replace All`

---

### 步骤 10：重新编译和测试

1. **在 IDEA 中重新加载 Maven 项目**
   - 右键主 pom.xml
   - `Maven` → `Reload project`

2. **重新构建项目**
   - `构建(Build)` → `重新构建项目(Rebuild Project)`

3. **启动服务测试**
   - 按顺序启动：Gateway → Account → Engine → Data
   - 启动前端：`pnpm dev`
   - 访问 `http://localhost:5173` 测试登录

---

## 📝 检查清单

完成后请检查：

- [ ] 所有 pom.xml 文件已修改
- [ ] 目录名已全部重命名
- [ ] Java 包名已全部重构为 `com.glen.autotest`
- [ ] Mapper XML 的 namespace 已更新
- [ ] 数据库已创建/重命名
- [ ] application.properties 已更新
- [ ] 前端配置已修改
- [ ] 原作者注释已清理
- [ ] 项目可以正常编译
- [ ] 所有服务可以正常启动
- [ ] 前端可以正常登录

---

## 🚨 常见问题

### 1. 编译错误：找不到类
- **原因**: 包名重构不完整
- **解决**: 在 IDEA 中重新搜索 `net.xdclass`，确保全部替换

### 2. 启动报错：ClassNotFoundException
- **原因**: Maven 依赖未更新
- **解决**: 重新 `Maven` → `Reload project` 和 `Rebuild Project`

### 3. 数据库连接失败
- **原因**: 数据库名未更新或配置文件未修改
- **解决**: 检查 application.properties 和数据库是否存在

### 4. Mapper 找不到
- **原因**: @MapperScan 注解的包名未更新
- **解决**: 搜索所有 @MapperScan，确保包名正确

---

## 💡 建议

1. **分步骤测试**: 完成每个步骤后都启动项目测试
2. **保留备份**: 至少保留备份 7 天
3. **Git 提交**: 如果使用 Git，每个步骤完成后提交一次
4. **文档更新**: 重命名完成后更新项目文档

---

**祝重命名顺利！如有问题请随时反馈。**

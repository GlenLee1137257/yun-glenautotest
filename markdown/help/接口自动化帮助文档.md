# 云测 接口自动化操作手册

## 目录

- [一、详细操作指南](#一详细操作指南)
  - [1.1 接口管理](#11-接口管理)
  - [1.2 用例管理](#12-用例管理)
  - [1.3 配置用例步骤](#13-配置用例步骤)
  - [1.4 配置关联取值](#14-配置关联取值)
  - [1.5 配置断言](#15-配置断言)
  - [1.6 执行测试用例](#16-执行测试用例)
  - [1.7 查看测试报告](#17-查看测试报告)
- [二、支持的功能](#二支持的功能)
  - [2.1 请求方法](#21-请求方法)
  - [2.2 请求参数类型](#22-请求参数类型)
  - [2.3 关联取值类型](#23-关联取值类型)
  - [2.4 断言类型](#24-断言类型)
  - [2.5 断言动作](#25-断言动作)
- [三、常见问题与排查](#三常见问题与排查)
  - [3.1 如何配置多步骤用例？](#31-如何配置多步骤用例)
  - [3.2 关联取值失败怎么办？](#32-关联取值失败怎么办)
  - [3.3 断言失败怎么办？](#33-断言失败怎么办)
  - [3.4 如何选择环境？](#34-如何选择环境)
- [四、最佳实践](#四最佳实践)
  - [4.1 用例设计建议](#41-用例设计建议)
  - [4.2 关联取值使用建议](#42-关联取值使用建议)
  - [4.3 断言配置建议](#43-断言配置建议)
  - [4.4 环境管理建议](#44-环境管理建议)

---

## 一、详细操作指南

### 1.1 接口管理

接口管理作为**接口文档库**，用于记录和管理项目中的接口定义，包括接口的基本信息、请求参数、请求头等。主要用途包括：

- **📚 接口文档**：记录项目中所有接口的基本信息，方便查阅
- **📊 接口统计**：统计项目中接口的数量、分布等信息
- **🔍 快速查询**：快速查找某个接口的定义和参数
- **👥 团队协作**：团队成员可以查看和了解项目中的接口

**注意事项：**
- 接口管理与用例管理是**独立的**，不强制关联
- 在创建测试用例时，直接在用例步骤中配置接口信息，更加灵活
- 接口管理主要用于记录和查阅，不影响用例的创建和执行

**操作路径：** 接口自动化 → 接口管理

#### 1.1.1 创建接口

**操作步骤：**

1. **进入接口管理页面**
   - 点击左侧菜单：**接口自动化 → 接口管理**
   - 点击 **新增接口** 按钮

2. **填写接口基本信息**
   - **接口名称**：接口的描述性名称（必填），例如："用户登录接口"、"获取商品列表"
   - **请求方法**：选择 HTTP 请求方法（必填），支持 GET、POST、PUT、DELETE、PATCH
   - **接口地址**：接口的路径（必填），例如：`/api/user/login`、`/api/products`
   - **所属模块**：选择接口所属的模块（必填），用于分类管理
   - **执行等级**：选择接口的重要程度（必填），P0（最高优先级）、P1、P2
   - **测试环境**：选择接口的测试环境（必填），例如：测试环境、预发布环境、生产环境
   - **接口描述**：接口的详细说明（必填），例如："用于用户登录验证的接口"

3. **配置请求参数**
   - **查询参数**：URL 查询参数，格式：`key=value`，多个参数用换行分隔
   - **请求头**：HTTP 请求头，格式：`key: value`，多个请求头用换行分隔
   - **REST 参数**：路径参数，格式：`key=value`，用于替换路径中的占位符
   - **请求体**：POST/PUT 请求的请求体内容
   - **请求体类型**：选择请求体的格式，支持 JSON、FORM、XML

4. **保存接口**
   - 点击页面底部的 **保存** 按钮
   - 接口会出现在接口列表中

**配置示例：**

**示例1：GET 请求（查询接口）**
```
接口名称：获取商品列表
请求方法：GET
接口地址：/api/products
查询参数：
  page=1
  limit=10
  category=electronics
请求头：
  Content-Type: application/json
  Authorization: Bearer token123
```

**示例2：POST 请求（创建接口）**
```
接口名称：用户登录
请求方法：POST
接口地址：/api/user/login
请求头：
  Content-Type: application/json
请求体：
{
  "username": "testuser",
  "password": "123456"
}
请求体类型：JSON
```

#### 1.1.2 编辑接口

1. 在接口列表中，找到要编辑的接口
2. 点击接口右侧的 **编辑** 按钮
3. 在弹出的对话框中修改接口信息
4. 点击 **确定** 保存修改

#### 1.1.3 删除接口

1. 在接口列表中，找到要删除的接口
2. 点击接口右侧的 **删除** 按钮
3. 在确认对话框中点击 **确认**
4. ⚠️ **注意**：删除接口不会影响已创建的用例，因为接口管理和用例管理是独立的

#### 1.1.4 接口管理的使用建议

**何时使用接口管理：**

- ✅ 记录项目中所有接口的基本信息，建立接口文档库
- ✅ 方便团队成员查阅和了解项目接口
- ✅ 统计和管理接口数量、分布等信息
- ✅ 作为参考，在创建用例时可以查阅接口信息

**接口管理 vs 用例管理：**

| 特性 | 接口管理 | 用例管理 |
|------|---------|---------|
| **主要用途** | 接口文档库，记录和查阅 | 测试用例，配置和执行 |
| **关联方式** | 可选关联（通过"从接口库选择"） | 独立或关联接口库 |
| **灵活性** | 固定的接口定义 | 可以自由配置或引用接口库 |
| **使用场景** | 文档记录、团队协作、快速填充 | 测试执行、自动化测试 |

**接口管理的两种使用方式：**

**方式1：作为接口文档库（独立使用）**
- 仅用于记录和查阅接口信息
- 不与用例管理关联
- 适合团队协作和接口文档管理

**方式2：作为接口配置源（与用例关联）**
- 在用例步骤中选择"从接口库选择"
- 自动同步接口库中的最新配置
- 适合多个用例使用相同接口的场景

**为什么不强制关联：**

1. **灵活性更高**：用例步骤可以选择引用接口库或手动配置，根据实际需求选择
2. **独立性更强**：同一个接口可以在不同用例中使用不同的参数、断言
3. **降低复杂度**：用例可以独立维护，不受接口库变更影响（如果选择手动配置）
4. **符合测试思维**：测试人员可以从"测试场景"出发，也可以从"接口"出发

**推荐使用场景：**

- **使用接口库**：接口稳定、多个用例共用、希望统一管理
- **手动配置**：接口不稳定、临时测试、需要自定义配置

#### 1.1.5 接口管理与用例步骤的字段同步规则

当你在**用例步骤**里把“使用接口库”开关打开，并通过“从接口库选择”关联某个接口后，系统会按照**分层规则**来处理字段，同步逻辑如下：

- **一层：强制同步字段（始终跟接口库走，步骤里不可编辑）**
  - **请求方式 `method`**
  - **接口地址 `path`**
  - **测试环境 `environmentId`**（推荐也跟接口库保持一致）
  - 特点：
    - 在步骤表单中，这几个字段会被锁定，不能手动修改；
    - 接口管理里修改接口的请求方式、地址或环境后，用例执行时会自动使用**最新值**。

- **二层：首次“快速填充”字段（只在选择接口时从接口库带入一次）**
  - **查询参数 `query`**
  - **请求头 `header`**
  - **请求体 `body`**
  - **请求体类型 `bodyType`**
  - 特点：
    - 选择接口的那一刻，会把接口管理中的这些配置一次性拷贝到步骤里，帮助你**快速初始化**；
    - 之后你在步骤中对这些字段的任何修改，都会**只保存在用例里**，不会被接口库的后续变更覆盖；
    - 换句话说，这些字段是“场景级”的，由每个用例自己维护。

- **三层：永不与接口库同步的字段（完全由用例控制）**
  - **步骤名称、描述、排序等**：`name`、`description`、`num`
  - **断言配置**：`assertion`
  - **关联取值配置**：`relation`
  - 特点：
    - 完全由测试用例控制，接口管理的任何修改都不会影响这些内容；
    - 你可以针对同一个接口，在不同用例中配置完全不同的断言和关联取值。

> 小结：接口管理更偏向“接口定义”，而用例管理更偏向“测试场景”。  

### 1.2 用例管理

用例管理用于创建和管理接口测试用例。一个用例可以包含多个步骤，每个步骤对应一个接口请求，步骤之间可以共享变量（通过关联取值）。

**操作路径：** 接口自动化 → 用例管理

#### 1.2.1 创建用例

**操作步骤：**

1. **进入用例管理页面**
   - 点击左侧菜单：**接口自动化 → 用例管理**
   - 点击 **新增接口用例** 按钮

2. **填写用例基本信息**
   - **用例名称**：测试用例的名称（必填），例如："用户登录流程测试"、"商品下单流程测试"
   - **所属模块**：用例所属的业务模块（必填），用于分类管理
   - **执行等级**：用例的重要程度（必填），P0（最高优先级）、P1、P2
   - **用例描述**：用例的详细说明（可选），例如："测试用户登录、获取用户信息、退出登录的完整流程"

3. **保存用例**
   - 点击页面底部的 **保存** 按钮
   - 用例会出现在用例列表中

**注意事项：**

- 用例名称要清晰明确，便于后续查找和管理
- 执行等级建议：P0（核心功能）、P1（重要功能）、P2（一般功能）
- 创建用例后，需要添加步骤才能执行

#### 1.2.2 编辑用例

1. 在用例列表中，找到要编辑的用例
2. 点击用例右侧的 **编辑** 按钮
3. 在用例编辑页面修改用例信息或步骤
4. 点击 **保存** 保存修改

#### 1.2.3 删除用例

1. 在用例列表中，找到要删除的用例
2. 点击用例右侧的 **删除** 按钮
3. 在确认对话框中点击 **确认**
4. ⚠️ **注意**：删除用例会同时删除用例下的所有步骤，此操作不可恢复

### 1.3 配置用例步骤

用例步骤是接口测试的核心，每个步骤对应一个接口请求。步骤按照排序号从小到大执行，排序号相同时按修改时间排序（修改时间越新的越先执行）。

**操作步骤：**

1. **进入用例编辑页面**
   - 在用例列表中，点击用例右侧的 **编辑** 按钮

2. **添加步骤**
   - 在"操作步骤列表"区域，点击 **新增操作** 按钮
   - 系统会弹出步骤配置对话框

3. **填写步骤基本信息**
   - **用例阶段名称**：步骤的描述性名称（必填），例如："登录接口"、"获取用户信息"
   - **排序**：步骤的执行顺序（必填），从 1 开始，数字越小越先执行
   - **用例阶段描述**：步骤的详细说明（可选）

4. **选择接口配置方式**
   
   系统提供两种接口配置方式：
   
   - **从接口库选择**（推荐⭐）：自动同步接口库中的最新配置
   - **手动配置**：自定义配置接口信息
   
   #### 方式1：从接口库选择（推荐⭐）
   
   **使用场景：**
   - 接口已在"接口管理"中定义
   - 希望接口信息自动同步更新
   - 多个用例使用相同的接口
   
   **操作步骤：**
   1. 在"使用接口库"处，将开关切换为 **从接口库选择**
   2. 点击 **选择接口** 按钮
   3. 在弹出的接口选择器中，选择要使用的接口
   4. 系统会自动填充接口的基本信息：
      - **强制同步并锁定的字段**：请求方法 `method`、接口地址 `path`、测试环境 `environmentId`
      - **一次性快速填充的字段**：查询参数 `query`、请求头 `header`、请求体 `body`、请求体类型 `bodyType`
   5. 其中 **method / path / environmentId 会被锁定**，其余快速填充字段可以根据用例场景继续手动调整
   
   **优势：**
   - ✅ **关键信息自动同步**：接口库中修改请求方法、接口地址或环境后，用例执行时会自动使用最新配置
   - ✅ **统一管理入口**：多个用例使用同一接口时，只需在接口库中维护接口定义
   - ✅ **参数可按场景微调**：查询参数、请求头、请求体等可以在每个步骤里根据测试场景单独调整，不会被接口库强行覆盖
   - ✅ **降低错误**：避免在每个用例里手动抄写接口地址、请求方式带来的拼写错误或遗漏
   
   **注意事项：**
   - 选择接口后，**请求方法、接口地址、测试环境**会被锁定，其它字段仍然可以在步骤中编辑
   - 如果接口库中的接口被删除，用例执行时会优先尝试使用步骤中已保存的信息
   - 可以随时切换回"手动配置"模式，此时步骤中的所有字段都由用例自己维护，与接口库完全解耦
   
   #### 方式2：手动配置
   
   **使用场景：**
   - 接口未在"接口管理"中定义
   - 需要临时测试某个接口
   - 需要自定义接口配置
   
   **操作步骤：**
   1. 在"使用接口库"处，保持开关为 **手动配置**
   2. 手动填写接口信息

5. **配置接口信息**
   - **请求方法**：选择 HTTP 请求方法（必填），支持 GET、POST、PUT、DELETE、PATCH
   - **接口地址**：接口的路径（必填），例如：`/api/user/login`
   - **执行等级**：步骤的重要程度（必填），P0、P1、P2
   - **测试环境**：选择步骤的测试环境（必填），不同步骤可以使用不同环境

6. **配置请求参数**
   - **查询参数**：URL 查询参数，格式：`key=value`，多个参数用换行分隔
   - **请求头**：HTTP 请求头，格式：`key: value`，多个请求头用换行分隔
   - **REST 参数**：路径参数，格式：`key=value`，用于替换路径中的占位符
   - **请求体**：POST/PUT 请求的请求体内容
   - **请求体类型**：选择请求体的格式，支持 JSON、FORM、XML

7. **配置关联取值（可选）**
   - 在"关联变量"标签页中，配置从响应中提取字段并保存为变量
   - 详细配置方法请参考 [1.4 配置关联取值](#14-配置关联取值)

8. **配置断言（可选）**
   - 在"断言"标签页中，配置响应验证规则
   - 详细配置方法请参考 [1.5 配置断言](#15-配置断言)

9. **保存步骤**
   - 点击 **确定** 按钮保存
   - 步骤会出现在步骤列表中

**使用关联变量：**

在步骤的路径、查询参数、请求体、断言预期值中，可以使用 `{{变量名}}` 引用之前步骤提取的变量。

**示例：**

步骤1：登录接口，提取 `token` 变量
```
关联取值：
  来源：RESPONSE_DATA
  类型：JSONPATH
  表达式：$.data.token
  变量名：token
```

步骤2：获取用户信息，使用 `token` 变量
```
请求头：
  Authorization: Bearer {{token}}
```

**注意事项：**

- 排序号可以相同，相同时按修改时间排序（修改时间越新的越先执行）
- 变量名区分大小写，使用时要保持一致
- 变量只在当前用例内有效，不同用例之间的变量不共享
- 如果引用的变量不存在，用例执行会失败

### 1.4 配置关联取值

关联取值用于从接口响应中提取字段并保存为变量，后续步骤可以通过 `{{变量名}}` 引用这些变量。这是实现多步骤用例数据传递的关键功能。

**关联取值的作用：**

- **数据传递**：将前一步的响应数据传递给下一步使用
- **动态参数**：使用动态数据（如 token、用户ID）作为后续请求的参数
- **减少硬编码**：避免在用例中硬编码数据，提高用例的可维护性

**配置步骤：**

1. **进入步骤配置对话框**
   - 在用例编辑页面，点击步骤右侧的 **编辑** 按钮
   - 切换到 **关联变量** 标签页

2. **添加关联取值规则**
   - 点击 **新增** 按钮
   - 填写关联取值配置：
     - **关联来源**：选择从哪个部分提取数据
       - `RESPONSE_CODE`：响应状态码
       - `RESPONSE_HEADER`：响应头
       - `RESPONSE_DATA`：响应体
       - `REQUEST_HEADER`：请求头
       - `REQUEST_BODY`：请求体
       - `REQUEST_QUERY`：查询参数
     - **关联类型**：选择提取方式
       - `JSONPATH`：使用 JSONPath 表达式提取 JSON 数据（推荐）
       - `REGEXP`：使用正则表达式提取文本数据
     - **关联表达式**：填写提取表达式
       - JSONPATH：例如 `$.data.token`、`$.data.user.id`
       - REGEXP：例如 `"token":"([^"]+)"`
     - **关联变量名**：变量的名称，后续步骤通过 `{{变量名}}` 引用

3. **保存关联取值规则**
   - 点击 **确定** 保存
   - 关联取值规则会出现在列表中

**配置示例：**

**示例1：从响应体中提取 token（JSONPATH）**

假设登录接口的响应为：
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userId": 12345
  }
}
```

配置关联取值：
```
关联来源：RESPONSE_DATA
关联类型：JSONPATH
关联表达式：$.data.token
关联变量名：token
```

后续步骤中使用：`Authorization: Bearer {{token}}`

**示例2：从响应体中提取用户ID（JSONPATH）**

配置关联取值：
```
关联来源：RESPONSE_DATA
关联类型：JSONPATH
关联表达式：$.data.userId
关联变量名：userId
```

后续步骤中使用：`/api/user/{{userId}}/profile`

**示例3：从响应头中提取 token（REGEXP）**

假设响应头中包含：`Set-Cookie: token=abc123; Path=/`

配置关联取值：
```
关联来源：RESPONSE_HEADER
关联类型：REGEXP
关联表达式：token=([^;]+)
关联变量名：token
```

**JSONPath 表达式说明：**

| 表达式 | 说明 | 示例 |
|--------|------|------|
| `$.data.token` | 获取 `data` 对象中的 `token` 字段 | `{"data": {"token": "abc"}}` → `"abc"` |
| `$.data.user.id` | 获取嵌套对象中的字段 | `{"data": {"user": {"id": 123}}}` → `123` |
| `$.data.items[0].name` | 获取数组第一个元素的字段 | `{"data": {"items": [{"name": "商品1"}]}}` → `"商品1"` |
| `$.data.items[*].id` | 获取数组中所有元素的字段 | `{"data": {"items": [{"id": 1}, {"id": 2}]}}` → `[1, 2]` |

**注意事项：**

- 变量名不能包含空格和特殊字符，建议使用字母、数字、下划线
- JSONPath 表达式必须以 `$` 开头
- 如果表达式匹配不到数据，用例执行会失败
- 变量会在步骤执行后立即保存，后续步骤可以直接使用

### 1.5 配置断言

断言用于验证接口响应是否符合预期，包括响应状态码、响应头、响应体等。如果断言失败，用例会被标记为失败。

**断言的作用：**

- **验证业务正确性**：确保接口返回的数据符合业务预期
- **验证接口稳定性**：确保接口在不同情况下都能正确响应
- **自动化验证**：无需人工检查，自动验证接口响应

**配置步骤：**

1. **进入步骤配置对话框**
   - 在用例编辑页面，点击步骤右侧的 **编辑** 按钮
   - 切换到 **断言** 标签页

2. **添加断言规则**
   - 点击 **新增** 按钮
   - 填写断言配置：
     - **断言来源**：选择从哪个部分获取数据进行比较
       - `RESPONSE_CODE`：响应状态码
       - `RESPONSE_HEADER`：响应头
       - `RESPONSE_DATA`：响应体
     - **断言类型**：选择提取方式
       - `JSONPATH`：使用 JSONPath 表达式提取 JSON 数据（推荐）
       - `REGEXP`：使用正则表达式提取文本数据
     - **断言动作**：选择比较方式
       - `EQUAL`：完全匹配
       - `NOT_EQUAL`：不完全匹配
       - `CONTAIN`：包含
       - `NOT_CONTAIN`：不包含
       - `GREAT_THEN`：大于（仅适用于数字）
       - `LESS_THEN`：小于（仅适用于数字）
     - **关联表达式**：填写提取表达式
       - JSONPATH：例如 `$.code`、`$.data.message`
       - REGEXP：例如 `"code":(\d+)`
     - **预期值**：期望的结果值，可以使用 `{{变量名}}` 引用变量
     - **说明**：断言的业务含义（可选），便于理解断言的目的

3. **保存断言规则**
   - 点击 **确定** 保存
   - 断言规则会出现在列表中

**配置示例：**

**示例1：验证响应状态码**

```
断言来源：RESPONSE_CODE
断言类型：REGEXP
断言动作：EQUAL
关联表达式：.*
预期值：200
说明：验证登录接口返回200状态码
```

**示例2：验证响应体中的 code 字段（JSONPATH）**

假设响应体为：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": 12345
  }
}
```

配置断言：
```
断言来源：RESPONSE_DATA
断言类型：JSONPATH
断言动作：EQUAL
关联表达式：$.code
预期值：200
说明：验证业务状态码为200
```

**示例3：验证响应体包含特定内容（JSONPATH）**

配置断言：
```
断言来源：RESPONSE_DATA
断言类型：JSONPATH
断言动作：CONTAIN
关联表达式：$.message
预期值：success
说明：验证响应消息包含"success"
```

**示例4：验证响应体中的数字字段（JSONPATH）**

配置断言：
```
断言来源：RESPONSE_DATA
断言类型：JSONPATH
断言动作：GREAT_THEN
关联表达式：$.data.userId
预期值：0
说明：验证用户ID大于0
```

**示例5：使用变量作为预期值**

假设步骤1提取了 `expectedUserId` 变量，步骤2验证用户ID：

配置断言：
```
断言来源：RESPONSE_DATA
断言类型：JSONPATH
断言动作：EQUAL
关联表达式：$.data.userId
预期值：{{expectedUserId}}
说明：验证用户ID与预期一致
```

**断言动作说明：**

| 断言动作 | 说明 | 适用场景 | 示例 |
|---------|------|---------|------|
| **EQUAL** | 完全匹配 | 精确值验证 | 状态码 = 200 |
| **NOT_EQUAL** | 不完全匹配 | 排除特定值 | 状态码 ≠ 404 |
| **CONTAIN** | 包含 | 模糊匹配 | 消息包含 "success" |
| **NOT_CONTAIN** | 不包含 | 排除特定内容 | 错误消息不包含 "error" |
| **GREAT_THEN** | 大于 | 数字比较 | 用户ID > 0 |
| **LESS_THEN** | 小于 | 数字比较 | 响应时间 < 1000 |

**注意事项：**

- 一个步骤可以配置多个断言，所有断言都通过才算成功
- 如果断言失败，用例会被标记为失败，并显示失败原因
- JSONPATH 表达式必须以 `$` 开头
- 数字比较（GREAT_THEN、LESS_THEN）仅适用于数字类型的值

### 1.6 执行测试用例

**操作步骤：**

1. **进入用例管理页面**
   - 点击左侧菜单：**接口自动化 → 用例管理**

2. **找到要执行的用例**
   - 在用例列表中找到目标用例
   - 可以使用搜索框按名称搜索

3. **执行用例**
   - 点击用例右侧的 **执行** 按钮
   - 系统会弹出确认对话框
   - 点击 **确认** 开始执行

4. **等待执行完成**
   - 执行过程中，可以在页面上看到执行状态
   - 执行完成后，会自动跳转到测试报告页面

**执行状态说明：**

- **执行中**：用例正在执行，请耐心等待
- **执行成功**：所有步骤都执行成功，所有断言都通过
- **执行失败**：有步骤执行失败或断言失败，查看报告了解详情

**注意事项：**

- 执行过程中不要关闭浏览器标签页
- 如果执行时间过长（超过1分钟），可能是接口响应慢或网络问题
- 执行失败时，查看报告中的错误信息，定位问题
- 用例会按照步骤的排序号顺序执行

### 1.7 查看测试报告

**报告入口：**

1. **执行完成后自动跳转**
   - 用例执行完成后，会自动跳转到报告详情页面

2. **从报告列表进入**
   - 点击左侧菜单：**测试报告 → 接口测试**
   - 在报告列表中找到目标报告
   - 点击用例名称进入详情

**报告内容说明：**

**1. 执行概览**
- 用例名称、执行时间、总耗时
- 总步骤数、成功步骤数、失败步骤数
- 整体执行状态（✅ 成功 / ❌ 失败）

**2. 步骤详情列表**
- 每个步骤的详细信息：
  - 步骤名称、请求方法、接口地址
  - 执行状态（✅ 成功 / ❌ 失败）
  - 执行耗时
  - 请求参数（查询参数、请求头、请求体）
  - 响应结果（状态码、响应头、响应体）
  - 断言结果（如果配置了断言）
  - 错误信息（如果失败）

**3. 查看请求和响应**
- 点击步骤可以展开查看详细的请求和响应信息
- 请求信息包括：请求方法、URL、请求头、请求体
- 响应信息包括：状态码、响应头、响应体

**4. 查看断言结果**
- 如果步骤配置了断言，会显示每个断言的结果
- 断言成功显示 ✅，断言失败显示 ❌ 并显示失败原因

**5. 查看关联取值结果**
- 如果步骤配置了关联取值，会显示提取的变量值
- 可以在后续步骤的请求中看到变量被替换后的实际值

**如何分析报告：**

1. **查看整体状态**
   - 用例是否执行成功
   - 哪些步骤成功，哪些步骤失败

2. **查看失败步骤**
   - 如果步骤失败，查看错误信息
   - 常见错误：接口不存在、参数错误、网络超时

3. **查看断言结果**
   - 如果断言失败，查看实际值和期望值的差异
   - 分析断言失败的原因

4. **查看响应数据**
   - 检查响应数据是否符合预期
   - 如果数据异常，可能是接口逻辑问题

---

## 二、支持的功能

### 2.1 请求方法

接口自动化支持以下 HTTP 请求方法：

| 请求方法 | 说明 | 使用场景 |
|---------|------|---------|
| **GET** | 获取资源 | 查询接口、列表接口 |
| **POST** | 创建资源 | 创建接口、登录接口 |
| **PUT** | 更新资源（完整更新） | 更新接口 |
| **DELETE** | 删除资源 | 删除接口 |
| **PATCH** | 更新资源（部分更新） | 部分更新接口 |

### 2.2 请求参数类型

接口自动化支持以下请求参数类型：

| 参数类型 | 说明 | 格式 | 示例 |
|---------|------|------|------|
| **查询参数** | URL 查询参数 | `key=value`，多个参数用换行分隔 | `page=1`<br>`limit=10` |
| **请求头** | HTTP 请求头 | `key: value`，多个请求头用换行分隔 | `Content-Type: application/json`<br>`Authorization: Bearer token123` |
| **REST 参数** | 路径参数 | `key=value`，用于替换路径中的占位符 | 路径：`/api/user/{userId}`<br>参数：`userId=123` |
| **请求体** | POST/PUT 请求的请求体 | JSON、FORM、XML 格式 | `{"username": "test", "password": "123456"}` |

**请求体类型：**

| 类型 | 说明 | 适用场景 |
|------|------|---------|
| **JSON** | JSON 格式数据 | RESTful API、前后端分离项目 |
| **FORM** | 表单格式数据 | 传统表单提交、文件上传 |
| **XML** | XML 格式数据 | SOAP 接口、传统 Web 服务 |

### 2.3 关联取值类型

关联取值支持以下类型：

| 类型 | 说明 | 适用场景 | 示例 |
|------|------|---------|------|
| **JSONPATH** | 使用 JSONPath 表达式提取 JSON 数据 | JSON 格式的响应 | `$.data.token` |
| **REGEXP** | 使用正则表达式提取文本数据 | 非 JSON 格式的响应、响应头 | `"token":"([^"]+)"` |

**关联来源：**

| 来源 | 说明 | 使用场景 |
|------|------|---------|
| **RESPONSE_CODE** | 响应状态码 | 提取状态码作为变量 |
| **RESPONSE_HEADER** | 响应头 | 提取 Cookie、Token 等 |
| **RESPONSE_DATA** | 响应体 | 提取业务数据（最常用） |
| **REQUEST_HEADER** | 请求头 | 提取请求头中的数据 |
| **REQUEST_BODY** | 请求体 | 提取请求体中的数据 |
| **REQUEST_QUERY** | 查询参数 | 提取查询参数中的数据 |

### 2.4 断言类型

断言支持以下类型：

| 类型 | 说明 | 适用场景 | 示例 |
|------|------|---------|------|
| **JSONPATH** | 使用 JSONPath 表达式提取 JSON 数据 | JSON 格式的响应 | `$.code`、`$.data.message` |
| **REGEXP** | 使用正则表达式提取文本数据 | 非 JSON 格式的响应、响应头 | `"code":(\d+)` |

**断言来源：**

| 来源 | 说明 | 使用场景 |
|------|------|---------|
| **RESPONSE_CODE** | 响应状态码 | 验证状态码是否为 200 |
| **RESPONSE_HEADER** | 响应头 | 验证响应头是否存在 |
| **RESPONSE_DATA** | 响应体 | 验证业务数据（最常用） |

### 2.5 断言动作

断言支持以下动作：

| 动作 | 说明 | 适用场景 | 示例 |
|------|------|---------|------|
| **EQUAL** | 完全匹配 | 精确值验证 | 状态码 = 200 |
| **NOT_EQUAL** | 不完全匹配 | 排除特定值 | 状态码 ≠ 404 |
| **CONTAIN** | 包含 | 模糊匹配 | 消息包含 "success" |
| **NOT_CONTAIN** | 不包含 | 排除特定内容 | 错误消息不包含 "error" |
| **GREAT_THEN** | 大于 | 数字比较 | 用户ID > 0 |
| **LESS_THEN** | 小于 | 数字比较 | 响应时间 < 1000 |

---

## 三、常见问题与排查

### 3.1 如何配置多步骤用例？

**问题：** 不知道如何配置包含多个步骤的用例。

**解决方案：**

1. **创建用例**
   - 在用例管理页面创建新用例

2. **添加步骤**
   - 在用例编辑页面，点击 **新增操作** 按钮
   - 配置每个步骤的接口信息、请求参数等

3. **设置排序**
   - 为每个步骤设置排序号（从 1 开始）
   - 步骤会按照排序号从小到大执行

4. **配置关联取值**
   - 在步骤中配置关联取值，提取响应数据
   - 后续步骤通过 `{{变量名}}` 使用提取的变量

**示例：**

```
步骤1：登录接口（排序：1）
  - 请求：POST /api/user/login
  - 关联取值：提取 token → {{token}}

步骤2：获取用户信息（排序：2）
  - 请求：GET /api/user/info
  - 请求头：Authorization: Bearer {{token}}

步骤3：退出登录（排序：3）
  - 请求：POST /api/user/logout
  - 请求头：Authorization: Bearer {{token}}
```

### 3.2 关联取值失败怎么办？

**问题：** 关联取值失败，提示"关联参数不存在"或"关联取值失败"。

**排查步骤：**

1. **检查表达式是否正确**
   - JSONPATH 表达式必须以 `$` 开头
   - 检查 JSON 结构是否与表达式匹配
   - 可以在在线 JSONPath 工具中测试表达式

2. **检查响应数据**
   - 查看测试报告中的响应数据
   - 确认响应中是否包含要提取的字段
   - 检查字段名是否正确（区分大小写）

3. **检查关联来源**
   - 确认关联来源选择正确（RESPONSE_DATA、RESPONSE_HEADER 等）
   - 如果从响应头提取，确认响应头中是否包含该字段

4. **检查变量名**
   - 变量名不能包含空格和特殊字符
   - 使用变量时，确保变量名完全一致（区分大小写）

**常见错误：**

- **表达式错误**：JSONPATH 表达式语法错误
- **字段不存在**：响应中不包含要提取的字段
- **类型不匹配**：提取的数据类型与预期不符

### 3.3 断言失败怎么办？

**问题：** 断言失败，不知道原因。

**排查步骤：**

1. **查看断言结果**
   - 在测试报告中查看断言结果
   - 查看实际值和期望值的差异

2. **检查表达式**
   - 确认断言表达式是否正确
   - JSONPATH 表达式必须以 `$` 开头
   - 检查 JSON 结构是否与表达式匹配

3. **检查预期值**
   - 确认预期值是否正确
   - 如果使用变量，确认变量值是否正确

4. **检查断言动作**
   - 确认断言动作是否合适
   - 数字比较（GREAT_THEN、LESS_THEN）仅适用于数字

**常见错误：**

- **表达式错误**：JSONPATH 或 REGEXP 表达式语法错误
- **值不匹配**：实际值与期望值不一致
- **类型错误**：使用数字比较但值不是数字

### 3.4 如何选择环境？

**问题：** 不知道如何选择测试环境。

**解决方案：**

1. **在用例步骤中选择环境**
   - 每个步骤可以独立选择测试环境
   - 不同步骤可以使用不同环境

2. **环境的作用**
   - 环境决定了接口的基础 URL（协议、域名、端口）
   - 不同环境可以有不同的配置（如测试环境、预发布环境、生产环境）

3. **环境配置**
   - 环境配置在系统管理中进行
   - 包含：环境名称、协议（http/https）、域名、端口

**使用建议：**

- 测试用例使用测试环境
- 回归测试使用预发布环境
- 生产环境仅用于生产验证（谨慎使用）

---

## 四、最佳实践

### 4.1 用例设计建议

**设计原则：**

1. **用例名称清晰**
   - 使用有意义的名称，便于后续查找和管理
   - 示例：`用户登录流程测试`、`商品下单流程测试`

2. **模块化管理**
   - 按业务模块组织用例（如"用户管理"、"商品管理"）
   - 便于管理和维护

3. **步骤顺序合理**
   - 按照业务流程顺序配置步骤
   - 确保数据依赖关系正确（前一步的数据能被后一步使用）

4. **步骤命名规范**
   - 步骤名称要清晰描述步骤的作用
   - 示例：`登录接口`、`获取用户信息`、`退出登录`

**用例设计示例：**

```
模块：用户管理
├── 用例1：用户登录流程测试
│   ├── 步骤1：登录接口（提取 token）
│   ├── 步骤2：获取用户信息（使用 token）
│   └── 步骤3：退出登录（使用 token）
├── 用例2：用户注册流程测试
│   ├── 步骤1：发送验证码
│   ├── 步骤2：注册用户（使用验证码）
│   └── 步骤3：验证注册结果
└── 用例3：用户信息更新测试
    ├── 步骤1：登录接口（提取 token）
    ├── 步骤2：更新用户信息（使用 token）
    └── 步骤3：验证更新结果
```

### 4.2 关联取值使用建议

**使用场景：**

1. **Token 传递**
   - 登录接口提取 token，后续接口使用 token 进行认证

2. **ID 传递**
   - 创建接口返回资源 ID，后续接口使用该 ID 进行操作

3. **动态数据**
   - 使用动态生成的数据（如订单号、验证码）作为后续请求的参数

**使用建议：**

- **变量命名规范**：使用有意义的变量名，如 `token`、`userId`、`orderId`
- **及时提取**：在数据产生后立即提取，避免数据丢失
- **验证提取结果**：在关键步骤验证提取的变量值是否正确
- **避免硬编码**：尽量使用关联取值，避免在用例中硬编码数据

**示例：**

```
步骤1：登录接口
  关联取值：
    - 提取 token → {{token}}
    - 提取 userId → {{userId}}

步骤2：创建订单（使用 userId）
  请求体：
    {
      "userId": {{userId}},
      "productId": 1001
    }

步骤3：查询订单（使用 token 和 orderId）
  请求头：
    Authorization: Bearer {{token}}
  查询参数：
    orderId={{orderId}}
```

### 4.3 断言配置建议

**配置原则：**

1. **必配置断言**
   - 响应状态码：验证请求是否成功（状态码 = 200）
   - 业务状态码：验证业务逻辑是否正确（code = 200）

2. **可选断言**
   - 响应数据：验证关键业务数据是否正确
   - 响应消息：验证响应消息是否符合预期

3. **断言阈值设置**
   - 根据业务需求设置合理的断言规则
   - 不要过于严格，避免误报

**推荐断言配置：**

```
断言1：响应状态码 = 200（必配）
断言2：业务状态码 = 200（必配，如果接口有业务状态码）
断言3：响应消息包含 "success"（可选，根据业务需求）
断言4：关键字段存在（可选，验证关键数据）
```

**示例：**

```
步骤：登录接口
断言：
  1. 响应状态码 = 200
  2. 业务状态码 = 200（JSONPATH: $.code）
  3. 响应消息包含 "登录成功"（JSONPATH: $.message, CONTAIN）
  4. token 存在（JSONPATH: $.data.token, CONTAIN）
```

### 4.4 环境管理建议

**环境配置建议：**

1. **环境命名规范**
   - 使用清晰的环境名称，如：`测试环境`、`预发布环境`、`生产环境`

2. **环境隔离**
   - 不同环境使用不同的域名和端口
   - 避免测试环境影响生产环境

3. **环境切换**
   - 在用例步骤中灵活选择环境
   - 不同步骤可以使用不同环境（如登录用测试环境，查询用预发布环境）

**使用建议：**

- **测试环境**：用于日常测试和开发验证
- **预发布环境**：用于回归测试和上线前验证
- **生产环境**：仅用于生产验证，谨慎使用

---

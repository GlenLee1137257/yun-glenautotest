# Glen UI 自动化 - 完整配置使用手册

## 目录

- [一、UI 自动化工作原理](#一ui-自动化工作原理)
  - [1.1 什么是 UI 自动化测试？](#11-什么是-ui-自动化测试)
  - [1.2 Glen UI 自动化执行流程](#12-glen-ui-自动化执行流程)
  - [1.3 UI 自动化断言机制详解](#13-ui-自动化断言机制详解)
    - [1.3.1 断言的工作原理](#131-断言的工作原理)
    - [1.3.2 断言执行流程](#132-断言执行流程)
    - [1.3.3 断言类型与底层实现](#133-断言类型与底层实现)
    - [1.3.4 断言成功/失败的判断标准](#134-断言成功失败的判断标准)
    - [1.3.5 断言结果记录](#135-断言结果记录)
    - [1.3.6 与接口自动化的对比](#136-与接口自动化的对比)
  - [1.4 核心组件](#14-核心组件)
- [二、技术架构说明](#二技术架构说明)
- [三、测试对象与数据来源](#三测试对象与数据来源)
- [四、完整配置流程](#四完整配置流程)
- [五、实战案例](#五实战案例)
- [六、当前支持与限制](#六当前支持与限制)
- [七、常见问题](#七常见问题)

---

## 一、UI 自动化工作原理

### 1.1 什么是 UI 自动化测试？

UI 自动化测试是通过程序模拟用户在浏览器中的操作（点击、输入、滚动等），自动执行测试用例，验证页面功能是否正常。

### 1.2 Glen UI 自动化执行流程

```
┌─────────────────────────────────────────────────────────────┐
│                     1. 用户配置测试用例                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 用例：登录功能测试                                    │   │
│  │   步骤1: 打开登录页面                                │   │
│  │   步骤2: 输入用户名                                  │   │
│  │   步骤3: 输入密码                                    │   │
│  │   步骤4: 点击登录按钮                                │   │
│  │   步骤5: 验证登录成功                                │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  2. Glen 引擎解析用例                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ • 读取用例配置                                       │   │
│  │ • 初始化测试报告                                     │   │
│  │ • 准备 Selenium WebDriver                           │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                 3. Selenium 驱动浏览器                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ • 启动浏览器（Chrome/Safari）                        │   │
│  │ • 执行每个步骤的操作                                 │   │
│  │ • 定位页面元素（通过 ID/XPath/CSS 等）              │   │
│  │ • 执行操作（点击/输入/断言）                         │   │
│  │ • 截图保存（如果配置）                               │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    4. 收集测试结果                           │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ • 记录每个步骤的执行状态（成功/失败）                │   │
│  │ • 记录断言结果                                       │   │
│  │ • 保存截图到 OSS                                     │   │
│  │ • 生成测试报告                                       │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  5. 展示测试报告                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ • 测试通过率                                         │   │
│  │ • 每个步骤的执行详情                                 │   │
│  │ • 失败步骤的截图                                     │   │
│  │ • 异常信息                                           │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 UI 自动化断言机制详解

#### 1.3.1 断言的工作原理

**UI 自动化的断言机制与接口自动化类似，但数据来源不同：**

| 对比项 | 接口自动化 | UI 自动化 |
|--------|----------|----------|
| **数据来源** | HTTP 响应数据（JSON/XML/Text） | 浏览器页面状态（DOM 元素、页面属性） |
| **获取方式** | 解析响应体 | 通过 Selenium WebDriver 操作 DOM |
| **断言对象** | 响应字段值 | 元素文本、元素属性、浏览器属性 |
| **比较方式** | 实际值 vs 期望值 | 实际值 vs 期望值 |
| **判断逻辑** | 完全相同 | 完全相同 |

#### 1.3.2 断言执行流程

```
┌─────────────────────────────────────────────────────────────┐
│                   1. 执行操作步骤                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 例如：点击登录按钮                                    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│             2. 获取页面实际状态（通过 Selenium）              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ • 元素文本：element.getText()                       │   │
│  │ • 浏览器标题：webDriver.getTitle()                  │   │
│  │ • 浏览器URL：webDriver.getCurrentUrl()              │   │
│  │ • 元素状态：element.isDisplayed() / isEnabled()     │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│             3. 与期望值进行比较（断言逻辑）                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 实际值："登录成功"                                    │   │
│  │ 期望值："登录成功"                                    │   │
│  │ 比较方式：相等（EQUAL）                               │   │
│  │ 结果：operationState = true                          │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│             4. 记录断言结果                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ • operationState: true/false                        │   │
│  │ • actualValue: 实际获取的值                          │   │
│  │ • expectValue: 配置的期望值                          │   │
│  │ • 如果失败，记录异常信息                              │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

#### 1.3.3 断言类型与底层实现

**1. 浏览器属性断言**

| 断言类型 | 获取实际值的方法 | 比较逻辑 |
|---------|----------------|---------|
| 网页标题相等 | `webDriver.getTitle()` | `Objects.equals(actual, expect)` |
| 网页标题包含 | `webDriver.getTitle()` | `actual.contains(expect)` |
| 网页URL相等 | `webDriver.getCurrentUrl()` | `Objects.equals(actual, expect)` |
| 网页URL包含 | `webDriver.getCurrentUrl()` | `actual.contains(expect)` |

**示例代码：**
```java
// 断言浏览器标题
String actualTitle = webDriver.getTitle();  // 获取实际标题："百度一下，你就知道"
String expectTitle = "百度一下";            // 期望标题
boolean result = actualTitle.contains(expectTitle);  // 判断：true
```

**2. 元素文本断言**

| 断言类型 | 获取实际值的方法 | 比较逻辑 |
|---------|----------------|---------|
| 元素文本相等 | `element.getText()` | `Objects.equals(actual, expect)` |
| 元素文本包含 | `element.getText()` | `actual.contains(expect)` |
| 元素文本大于 | `Long.parseLong(element.getText())` | `actual > expect` |
| 元素文本小于 | `Long.parseLong(element.getText())` | `actual < expect` |

**示例代码：**
```java
// 断言元素文本
WebElement element = driver.findElement(By.id("result"));
String actualText = element.getText();  // 获取实际文本："登录成功"
String expectText = "登录成功";         // 期望文本
boolean result = Objects.equals(actualText, expectText);  // 判断：true
```

**3. 元素状态断言**

| 断言类型 | 获取实际值的方法 | 判断逻辑 |
|---------|----------------|---------|
| 元素存在 | `findElement()` 是否抛出异常 | 找到元素 = true，异常 = false |
| 元素不存在 | `findElement()` 是否抛出异常 | 找到元素 = false，异常 = true |
| 元素可见 | `element.isDisplayed()` | 返回 true/false |
| 元素不可见 | `element.isDisplayed()` | 返回 !isDisplayed() |
| 元素启用 | `element.isEnabled()` | 返回 true/false |
| 元素禁用 | `element.isEnabled()` | 返回 !isEnabled() |
| 元素选中 | `element.isSelected()` | 返回 true/false |
| 元素未选中 | `element.isSelected()` | 返回 !isSelected() |

**示例代码：**
```java
// 断言元素存在
try {
    WebElement element = driver.findElement(By.id("loginBtn"));
    boolean result = true;  // 元素存在
} catch (NoSuchElementException e) {
    boolean result = false;  // 元素不存在
}

// 断言元素可见
WebElement element = driver.findElement(By.id("message"));
boolean result = element.isDisplayed();  // true = 可见，false = 不可见
```

#### 1.3.4 断言成功/失败的判断标准

**断言成功的条件：**

1. **操作执行成功**：`executeState = true`
   - 元素定位成功
   - 操作执行无异常

2. **断言验证通过**：`assertionState = true`
   - 实际值与期望值比较结果为 `true`
   - 元素状态符合预期

**断言失败的场景：**

1. **操作执行失败**：`executeState = false`
   - 元素定位失败（找不到元素）
   - 操作执行异常（如点击失败）

2. **断言验证失败**：`assertionState = false`
   - 实际值与期望值不匹配
   - 元素状态不符合预期

**完整判断逻辑：**
```java
// 步骤最终状态 = 操作状态 AND 断言状态
boolean stepSuccess = executeState && assertionState;

// 如果断言失败且配置了"失败后不继续"
if (!assertionState && !isContinue) {
    // 停止执行后续步骤
    return result;
}
```

#### 1.3.5 断言结果记录

**每个断言步骤都会记录以下信息：**

```java
UiOperationResultDTO {
    operationState: true/false,      // 断言是否通过
    actualValue: "实际获取的值",      // 实际值（用于报告）
    expectValue: "期望值",          // 期望值（用于报告）
    operationType: "ASSERTION_..."  // 断言类型
}
```

**测试报告中的展示：**

- ✅ **断言通过**：显示实际值和期望值，标记为成功
- ❌ **断言失败**：显示实际值和期望值，标记为失败，并记录异常信息

#### 1.3.6 与接口自动化的对比

| 特性 | 接口自动化 | UI 自动化 |
|------|----------|----------|
| **数据获取** | HTTP 响应解析 | Selenium DOM 操作 |
| **数据格式** | JSON/XML/Text | 字符串/布尔值/数字 |
| **断言方式** | JSONPath/XPath 提取 | 直接获取元素属性 |
| **执行速度** | 快（毫秒级） | 慢（秒级，需要渲染） |
| **稳定性** | 高（接口稳定） | 较低（页面可能变化） |
| **适用场景** | API 功能测试 | 前端功能、UI 交互测试 |

**总结：**

- **接口自动化**：通过 HTTP 请求获取响应数据，解析后与期望值比较
- **UI 自动化**：通过 Selenium WebDriver 操作浏览器，获取页面元素的实际状态，与期望值比较
- **底层逻辑相同**：都是"获取实际值 → 与期望值比较 → 返回布尔结果"

### 1.4 核心组件

| 组件 | 作用 | 技术栈 |
|------|------|--------|
| **前端界面** | 配置用例、查看报告 | Vue 3 + Ant Design Vue |
| **后端引擎** | 解析用例、调度执行 | Spring Boot |
| **Selenium WebDriver** | 驱动浏览器 | Selenium 4.x |
| **浏览器驱动** | 与浏览器通信 | ChromeDriver / SafariDriver |
| **测试报告** | 存储结果 | MySQL + Kafka |

---

## 二、技术架构说明

### 2.1 当前支持的平台

| 平台类型 | 支持状态 | 说明 |
|---------|---------|------|
| **Web 浏览器** | ✅ 完全支持 | 通过 Selenium WebDriver |
| **桌面应用** | ❌ 不支持 | 需要集成 WinAppDriver |
| **移动端 App** | ❌ 不支持 | 需要集成 Appium |

**当前版本只支持 Web 浏览器自动化测试。**

### 2.2 支持的浏览器

| 浏览器 | 支持状态 | 驱动版本要求 |
|--------|---------|-------------|
| **Chrome** | ✅ 支持 | ChromeDriver 与浏览器版本匹配 |
| **Safari** | ✅ 支持 | macOS 内置 SafariDriver |
| Firefox | ⚠️ 理论支持 | 需配置 GeckoDriver |
| Edge | ⚠️ 理论支持 | 需配置 EdgeDriver |

### 2.3 为什么不支持移动端？

**当前架构限制：**
```
Glen UI 自动化 → Selenium WebDriver → 浏览器
                     ↓
              只支持桌面浏览器
```

**如需支持移动端，需要：**
```
Glen UI 自动化 → Appium → Android/iOS 设备
                   ↓
          支持原生 App + 移动浏览器
```

**未来规划：**
- 🔄 集成 Appium Server
- 🔄 支持 Android 真机/模拟器
- 🔄 支持 iOS 真机/模拟器
- 🔄 支持移动端 Web（H5）

---

## 三、测试对象与数据来源

### 3.1 测试对象的选择

#### ✅ 推荐测试对象

| 测试对象 | 说明 | 优点 | 缺点 |
|---------|------|------|------|
| **公开演示网站** | 各种测试练习网站 | 稳定、无需维护 | 功能有限 |
| **自己的项目前端** | 本项目的 Vue 前端 | 真实业务场景 | 需要运行环境 |
| **开源项目** | GitHub 上的开源项目 | 功能丰富 | 可能频繁更新 |
| **测试专用网站** | 专门用于自动化测试 | 专业、完善 | 部分需要付费 |

#### 📋 推荐的测试网站（贴近在线教育平台业务场景）

1. **OrangeHRM（人力资源管理平台）⭐ 最推荐**
   - URL: https://opensource-demo.orangehrmlive.com/
   - 特点：真实的企业级管理系统，功能完整，界面专业
   - 业务场景：员工管理、考勤、培训管理、绩效评估
   - 测试账号：`Admin` / `admin123`
   - 适用场景：企业管理系统、用户权限、数据管理、表单操作
   - 推荐理由：界面专业、功能丰富、贴近真实业务，适合学习管理系统类UI测试

2. **ParaBank（银行系统演示）**
   - URL: https://parabank.parasoft.com/
   - 特点：完整的银行系统，包含账户管理、转账、支付等
   - 业务场景：用户注册、账户查询、转账汇款、账单查询
   - 测试账号：注册后自动创建测试账户
   - 适用场景：金融业务、账户管理、交易流程、数据验证
   - 推荐理由：金融业务场景，数据敏感度高，适合测试数据验证和表单操作

3. **Swag Labs（电商购物平台）**
   - URL: https://www.saucedemo.com/
   - 特点：完整的电商购物流程，界面简洁清晰
   - 业务场景：用户登录、商品浏览、购物车、订单结算
   - 测试账号：
     - `standard_user` / `secret_sauce`（正常用户）
     - `problem_user` / `secret_sauce`（问题用户，用于异常测试）
     - `performance_glitch_user` / `secret_sauce`（性能测试用户）
   - 适用场景：电商业务、购物流程、商品管理、订单处理
   - 推荐理由：流程完整、多用户类型、适合端到端测试和业务流程验证

4. **DemoQA（在线教育平台风格）**
   - URL: https://demoqa.com/
   - 特点：现代化UI设计，包含表单、交互、数据展示等，类似在线教育平台
   - 业务场景：用户注册、表单填写、数据展示、文件上传、交互操作
   - 适用场景：表单验证、数据录入、UI交互、文件处理、学习管理系统
   - 推荐理由：UI现代化、元素丰富、适合UI测试练习，界面风格贴近在线教育平台

5. **Guru99 Bank（银行系统）**
   - URL: https://demo.guru99.com/V4/
   - 特点：银行管理系统，包含账户、转账、支付等功能
   - 业务场景：用户登录、账户查询、转账、支付、报表查询
   - 测试账号：`mngr123456` / `mUtUqAs`
   - 适用场景：金融业务、账户管理、报表查询、数据统计
   - 推荐理由：银行系统场景，功能完整，适合业务测试和数据管理场景

6. **本项目前端（Glen自动化云测平台）**
   - URL: http://localhost:5173
   - 特点：真实业务场景，与平台功能完全匹配
   - 业务场景：用户登录、项目管理、用例编辑、测试报告查看
   - 适用场景：平台功能测试、真实业务验证、回归测试
   - 推荐理由：完全贴合项目业务，测试结果可直接用于项目改进

### 3.2 测试数据的获取

#### 方式一：使用网站自带的测试账号

```yaml
# 示例1：OrangeHRM（人力资源管理平台）
URL: https://opensource-demo.orangehrmlive.com/
测试账号：
  - Admin / admin123

# 示例2：Swag Labs（电商购物平台）
URL: https://www.saucedemo.com/
测试账号：
  - standard_user / secret_sauce（正常用户）
  - problem_user / secret_sauce（问题用户，用于异常测试）
  - performance_glitch_user / secret_sauce（性能测试用户）

# 示例3：Guru99 Bank（银行系统）
URL: https://demo.guru99.com/V4/
测试账号：
  - mngr123456 / mUtUqAs
```

#### 方式二：使用公开数据

```yaml
# 示例：搜索测试
网站：百度 (https://www.baidu.com)
测试数据：
  - 搜索关键词："自动化测试"
  - 搜索关键词："Selenium"
```

#### 方式三：使用本项目数据

```yaml
# 如果测试 Glen 平台本身
URL: http://localhost:5173
测试账号：从数据库中查询或手动注册
测试数据：
  - 项目名称
  - 用例名称
  - 步骤配置
```

### 3.3 与接口测试的区别

| 对比项 | 接口自动化 | UI 自动化 |
|--------|-----------|-----------|
| **测试对象** | API 接口 | Web 页面 |
| **数据来源** | Mock 数据 / 真实接口 | 真实网页 |
| **执行速度** | 快（毫秒级） | 慢（秒级） |
| **稳定性** | 高 | 较低（页面变化影响大） |
| **依赖环境** | 低 | 高（需要浏览器） |
| **适用场景** | 后端逻辑验证 | 用户体验验证 |

**为什么 UI 测试不能用 Mock？**
- UI 测试需要真实的页面渲染
- 需要验证用户交互行为
- 需要检查页面元素的显示效果
- 依赖浏览器的 DOM 结构

---

## 四、完整配置流程

### 4.1 环境准备

#### 步骤 1：确认浏览器已安装

```bash
# 检查 Chrome 版本（推荐）
google-chrome --version
# 或
chrome --version

# macOS 检查 Safari
/Applications/Safari.app/Contents/MacOS/Safari --version
```

#### 步骤 2：安装浏览器驱动（如需要）

**自动管理（推荐）：**
Glen 平台已集成 WebDriver Manager，会自动下载匹配的驱动。

**手动安装：**
```bash
# 下载 ChromeDriver
# 访问：https://chromedriver.chromium.org/
# 选择与 Chrome 版本匹配的驱动

# 解压并添加到 PATH
unzip chromedriver_linux64.zip
sudo mv chromedriver /usr/local/bin/
sudo chmod +x /usr/local/bin/chromedriver
```

#### 步骤 3：确认 Glen 服务运行正常

```bash
# 检查后端服务
curl http://localhost:8083/actuator/health

# 检查前端服务
curl http://localhost:5173
```

### 4.2 创建第一个 UI 测试用例

#### 场景：测试百度搜索功能

**重要说明：操作与步骤的关系**

在 Glen UI 自动化中：
- **一个操作 = 一个步骤**
- 每个步骤对应一个具体的操作（如"打开窗口"、"键盘输入"、"鼠标点击"等）
- 测试用例由多个步骤按顺序组成
- 执行时会按照步骤顺序依次执行

**操作步骤：**
1. 点击 **"新增操作"** 按钮 → 创建一个新步骤
2. 在弹出的对话框中配置该步骤的所有信息
3. 保存后，该步骤会出现在步骤列表中
4. 重复以上步骤，添加所有需要的步骤

**用例配置：**

| 字段 | 值 |
|------|---|
| 用例名称 | 百度搜索功能测试 |
| 所属模块 | 搜索功能测试 |
| 浏览器 | Chrome |
| 执行等级 | P0 |
| 描述 | 验证百度搜索基本功能 |

**步骤配置（共 8 个步骤，需要点击 8 次"新增操作"）：**

| 步骤序号 | 操作类型 | 定位方式 | 定位表达式 | 值 | 描述 |
|---------|---------|---------|-----------|-----|------|
| 步骤1 | 打开窗口 | - | - | https://www.baidu.com | 打开百度首页 |
| 步骤2 | 强制等待 | - | - | 2000 | 等待页面加载（2秒） |
| 步骤3 | 键盘输入 | ID | kw | Selenium自动化测试 | 在搜索框输入关键词 |
| 步骤4 | 强制等待 | - | - | 500 | 等待输入完成 |
| 步骤5 | 鼠标左键点击 | ID | su | - | 点击搜索按钮 |
| 步骤6 | 强制等待 | - | - | 3000 | 等待搜索结果加载 |
| 步骤7 | 网页标题包含 | - | - | Selenium自动化测试 | 验证标题包含搜索词 |
| 步骤8 | 元素存在 | CSS选择器 | #content_left | - | 验证搜索结果区域存在 |

**配置示例：**

**示例1：步骤1 - 打开窗口**

```
1. 点击"新增操作"按钮
2. 在弹出的对话框中：
   - 名称：打开窗口
   - 步骤类型：本地步骤（默认）
   - 操作类型：在级联选择器中选择"浏览器" → "打开窗口"
   - 值：https://www.baidu.com
   - 描述：打开百度首页（可选）
   - 是否截图：根据需要选择（可选）
   - 失败是否继续：根据需要选择（可选）
3. 点击"确定"保存
```

**注意：** "打开窗口"操作不需要配置定位方式和定位表达式，只需要填写"值"字段（URL地址）。

**示例2：步骤3 - 键盘输入**

```
1. 点击"新增操作"按钮
2. 在弹出的对话框中：
   - 名称：输入关键词
   - 步骤类型：本地步骤（默认）
   - 操作类型：在级联选择器中选择"键盘" → "键盘输入"
   - 定位方式：ID
   - 定位表达式：kw
   - 值：Selenium自动化测试
   - 描述：在搜索框输入关键词（可选）
3. 点击"确定"保存
```

**注意：** "键盘输入"操作需要配置定位方式、定位表达式和值。

**示例3：步骤2 - 强制等待**

```
1. 点击"新增操作"按钮
2. 在弹出的对话框中：
   - 名称：等待页面加载
   - 步骤类型：本地步骤（默认）
   - 操作类型：在级联选择器中选择"等待" → "强制等待"
   - 值：2000（单位：毫秒，即2秒）
   - 描述：等待页面加载（可选）
3. 点击"确定"保存
```

**注意：** "强制等待"操作只需要填写"值"字段（等待时间，单位：毫秒），不需要定位方式。

**操作类型选择说明：**

操作类型使用**级联选择器**（Cascader）选择：
- 第一级：选择操作分类（浏览器、鼠标、键盘、等待、断言等）
- 第二级：选择具体操作（打开窗口、鼠标左键点击、键盘输入等）

选择操作类型后，系统会根据操作类型自动显示需要的字段：
- 有些操作只需要"值"字段（如：打开窗口、强制等待）
- 有些操作需要"定位方式"+"定位表达式"+"值"（如：键盘输入、鼠标点击）
- 有些操作需要"定位方式"+"定位表达式"+"预期值"（如：断言操作）

**步骤管理说明：**

- **步骤顺序**：步骤会按照"排序"（num）字段自动排序，执行时按此顺序执行
- **编辑步骤**：点击步骤列表中的"编辑"按钮，可以修改步骤配置
- **删除步骤**：点击步骤列表中的"删除"按钮，可以删除该步骤
- **步骤复用**：可以使用"引用步骤"功能，引用其他用例中的步骤（详见步骤复用功能说明）

**如何获取定位表达式？**

1. 打开浏览器开发者工具（F12）
2. 点击元素选择器图标
3. 点击要定位的页面元素
4. 在 Elements 面板查看：
   - `id` 属性 → 使用 ID 定位
   - `class` 属性 → 使用 CLASS_NAME 定位
   - 右键 → Copy → Copy selector → 使用 CSS_SELECTOR 定位
   - 右键 → Copy → Copy XPath → 使用 XPATH 定位

### 4.3 执行测试用例

#### 操作步骤：

1. 进入 **UI自动化 → 用例管理**
2. 找到刚创建的用例
3. 点击 **执行** 按钮
4. 等待执行完成（浏览器会自动打开并执行操作）
5. 查看测试报告

#### 执行过程中会发生什么？

```
1. Glen 后端接收执行请求
2. 初始化 Selenium WebDriver
3. 启动 Chrome 浏览器
4. 依次执行每个步骤：
   ├─ 打开百度首页
   ├─ 等待 2 秒
   ├─ 定位搜索框（通过 id=kw）
   ├─ 输入关键词
   ├─ 定位搜索按钮（通过 id=su）
   ├─ 点击按钮
   ├─ 等待 3 秒
   ├─ 验证标题
   └─ 验证搜索结果区域存在
5. 收集执行结果
6. 保存测试报告
7. 关闭浏览器
```

### 4.4 查看测试报告

#### 报告内容：

- ✅ 测试用例名称
- ✅ 执行时间
- ✅ 总步骤数
- ✅ 成功步骤数
- ✅ 失败步骤数
- ✅ 每个步骤的详细信息：
  - 步骤名称
  - 执行状态（成功/失败）
  - 耗时
  - 截图（如果配置）
  - 异常信息（如果失败）

---

## 五、实战案例

### 案例 1：测试登录功能（OrangeHRM 人力资源管理平台）

**测试网站：** https://opensource-demo.orangehrmlive.com/

**业务场景：** 企业HR管理系统登录验证

**测试场景：** 验证正确的用户名和密码可以成功登录并跳转到仪表盘

**步骤配置：**

```yaml
用例名称: HR系统登录功能测试
浏览器: Chrome
等级: P0

步骤列表:
  步骤1:
    操作类型: 打开窗口
    值: https://opensource-demo.orangehrmlive.com/
    描述: 打开HR系统首页
  
  步骤2:
    操作类型: 强制等待
    值: 3000
    描述: 等待页面加载
  
  步骤3:
    操作类型: 键盘输入
    定位方式: NAME
    定位表达式: username
    值: Admin
    描述: 输入管理员用户名
  
  步骤4:
    操作类型: 键盘输入
    定位方式: NAME
    定位表达式: password
    值: admin123
    描述: 输入密码
  
  步骤5:
    操作类型: 鼠标左键点击
    定位方式: CSS选择器
    定位表达式: button[type='submit']
    描述: 点击登录按钮
  
  步骤6:
    操作类型: 强制等待
    值: 3000
    描述: 等待登录完成
  
  步骤7:
    操作类型: 网页URL包含
    预期值: dashboard
    描述: 验证跳转到仪表盘页面
  
  步骤8:
    操作类型: 元素文本包含
    定位方式: XPATH
    定位表达式: //h6[contains(@class,'oxd-text')]
    预期值: Dashboard
    描述: 验证页面标题显示Dashboard
    是否截图: true
```

### 案例 2：测试银行账户注册（ParaBank 银行系统）

**测试网站：** https://parabank.parasoft.com/

**业务场景：** 银行系统用户注册和账户管理

**测试场景：** 用户注册 → 创建账户 → 验证账户信息

**步骤配置：**

```yaml
用例名称: 银行账户注册测试
浏览器: Chrome
等级: P0

步骤列表:
  步骤1:
    操作类型: 打开窗口
    值: https://parabank.parasoft.com/
    描述: 打开银行系统首页
  
  步骤2:
    操作类型: 强制等待
    值: 2000
    描述: 等待页面加载
  
  步骤3:
    操作类型: 鼠标左键点击
    定位方式: LINK_TEXT
    定位表达式: Register
    描述: 点击注册链接
  
  步骤4:
    操作类型: 强制等待
    值: 2000
    描述: 等待注册页面加载
  
  步骤5:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.firstName
    值: 张
    描述: 输入名字
  
  步骤6:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.lastName
    值: 三
    描述: 输入姓氏
  
  步骤7:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.address.street
    值: 北京市朝阳区
    描述: 输入地址
  
  步骤8:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.address.city
    值: 北京
    描述: 输入城市
  
  步骤9:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.address.state
    值: 北京
    描述: 输入省份
  
  步骤10:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.address.zipCode
    值: 100000
    描述: 输入邮编
  
  步骤11:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.phoneNumber
    值: 13800138000
    描述: 输入电话
  
  步骤12:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.ssn
    值: 123456789
    描述: 输入SSN
  
  步骤13:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.username
    值: testuser001
    描述: 输入用户名
  
  步骤14:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: customer.password
    值: password123
    描述: 输入密码
  
  步骤15:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: repeatedPassword
    值: password123
    描述: 确认密码
  
  步骤16:
    操作类型: 鼠标左键点击
    定位方式: CSS选择器
    定位表达式: input[value='Register']
    描述: 点击注册按钮
  
  步骤17:
    操作类型: 强制等待
    值: 3000
    描述: 等待注册完成
  
  步骤18:
    操作类型: 元素文本包含
    定位方式: XPATH
    定位表达式: //h1[@class='title']
    预期值: Welcome
    描述: 验证注册成功，显示欢迎信息
    是否截图: true
```

### 案例 3：测试电商购物流程（Swag Labs）

**测试网站：** https://www.saucedemo.com/

**业务场景：** 电商购物完整流程测试

**测试场景：** 用户登录 → 浏览商品 → 加入购物车 → 查看购物车 → 填写订单信息 → 完成结账

**步骤配置：**

```yaml
用例名称: 电商购物流程测试
浏览器: Chrome
等级: P0

步骤列表:
  # 登录部分
  步骤1:
    操作类型: 打开窗口
    值: https://www.saucedemo.com/
  
  步骤2:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: user-name
    值: standard_user
  
  步骤3:
    操作类型: 键盘输入
    定位方式: ID
    定位表达式: password
    值: secret_sauce
  
  步骤4:
    操作类型: 鼠标左键点击
    定位方式: ID
    定位表达式: login-button
  
  步骤5:
    操作类型: 强制等待
    值: 2000
  
  # 添加商品到购物车
  步骤6:
    操作类型: 鼠标左键点击
    定位方式: CSS选择器
    定位表达式: button[id='add-to-cart-sauce-labs-backpack']
    描述: 添加第一个商品
  
  步骤7:
    操作类型: 鼠标左键点击
    定位方式: CSS选择器
    定位表达式: .shopping_cart_link
    描述: 进入购物车
  
  步骤8:
    操作类型: 强制等待
    值: 1000
  
  # 验证购物车
  步骤9:
    操作类型: 元素存在
    定位方式: CLASS_NAME（通过元素的 class 名称定位）
    定位表达式: cart_item（表示购物车中商品条目的 class 名）
    描述: 验证商品在购物车中
    是否截图: true
  
  # 结账
  步骤10:
    操作类型: 鼠标左键点击
    定位方式: ID（通过元素的唯一 ID 属性定位）
    定位表达式: checkout（结账按钮的 ID）
  
  步骤11:
    操作类型: 键盘输入
    定位方式: ID（通过元素的唯一 ID 属性定位）
    定位表达式: first-name（收货人“名字”输入框的 ID）
    值: 张
  
  步骤12:
    操作类型: 键盘输入
    定位方式: ID（通过元素的唯一 ID 属性定位）
    定位表达式: last-name（收货人“姓氏”输入框的 ID）
    值: 三
  
  步骤13:
    操作类型: 键盘输入
    定位方式: ID（通过元素的唯一 ID 属性定位）
    定位表达式: postal-code（邮编输入框的 ID）
    值: 100000
  
  步骤14:
    操作类型: 鼠标左键点击
    定位方式: ID（通过元素的唯一 ID 属性定位）
    定位表达式: continue（继续结账按钮的 ID）
  
  步骤15:
    操作类型: 强制等待
    值: 1000
  
  步骤16:
    操作类型: 鼠标左键点击
    定位方式: ID
    定位表达式: finish
  
  步骤17:
    操作类型: 强制等待
    值: 2000
  
  # 验证订单完成
  步骤18:
    操作类型: 元素文本包含
    定位方式: CLASS_NAME
    定位表达式: complete-header
    预期值: Thank you
    描述: 验证订单完成提示
    是否截图: true
```

---

## 六、当前支持与限制

### 6.1 支持的操作类型

#### 浏览器操作

| 操作 | 说明 | 示例 |
|------|------|------|
| 打开窗口 | 打开指定 URL | https://www.baidu.com |
| 关闭窗口 | 关闭当前窗口 | - |
| 最大化窗口 | 最大化浏览器 | - |
| 设置窗口大小 | 设置宽高 | 1920x1080 |
| 浏览器前进 | 前进到下一页 | - |
| 浏览器后退 | 返回上一页 | - |
| 浏览器刷新 | 刷新当前页面 | - |
| 切换窗口 | 切换浏览器标签页 | - |

#### 鼠标操作

| 操作 | 说明 | 需要定位 |
|------|------|---------|
| 左键单击 | 单击元素 | ✅ |
| 右键点击 | 右键菜单 | ✅ |
| 双击 | 双击元素 | ✅ |
| 移入元素 | 鼠标悬停 | ✅ |
| 拖拽元素 | 拖拽到目标 | ✅ |

#### 键盘操作

| 操作 | 说明 | 需要定位 |
|------|------|---------|
| 键盘输入 | 输入文本 | ✅ |
| 清空输入框 | 清空内容 | ✅ |
| 表单提交 | 提交表单 | ✅ |

#### 等待操作

| 操作 | 说明 | 参数 |
|------|------|------|
| 强制等待 | 固定等待时间 | 毫秒数 |
| 隐式等待 | 等待元素出现 | 毫秒数 |

#### 断言操作

| 断言类型 | 说明 | 示例 |
|---------|------|------|
| 网页标题相等 | 标题完全匹配 | "百度一下" |
| 网页标题包含 | 标题包含关键词 | "百度" |
| 网页URL相等 | URL 完全匹配 | "https://www.baidu.com" |
| 网页URL包含 | URL 包含关键词 | "/search" |
| 元素文本相等 | 元素文本完全匹配 | "登录成功" |
| 元素文本包含 | 元素文本包含关键词 | "成功" |
| 元素存在 | 元素存在于页面 | - |
| 元素不存在 | 元素不存在 | - |
| 元素可见 | 元素可见 | - |
| 元素不可见 | 元素不可见 | - |
| 元素启用 | 元素可交互 | - |
| 元素禁用 | 元素不可交互 | - |

### 6.2 支持的定位方式

| 定位方式 | 说明 | 示例 |
|---------|------|------|
| **ID** | 通过 id 属性定位 | `kw` |
| **NAME** | 通过 name 属性定位 | `username` |
| **XPATH** | 通过 XPath 定位 | `//input[@name='kw']` |
| **CSS_SELECTOR** | 通过 CSS 选择器定位 | `#kw` 或 `.btn-primary` |
| **CLASS_NAME** | 通过 class 定位 | `btn-primary` |
| **TAG_NAME** | 通过标签名定位 | `button` |
| **LINK_TEXT** | 通过链接文本定位 | `登录` |
| **PARTIAL_LINK_TEXT** | 通过部分链接文本定位 | `登` |

**推荐优先级：**
1. ID（最稳定）
2. CSS_SELECTOR（灵活且快速）
3. XPATH（功能强大但性能较低）
4. 其他方式

### 6.3 当前限制

| 限制项 | 说明 | 解决方案 |
|--------|------|---------|
| ❌ 不支持移动端 | 只支持桌面浏览器 | 未来集成 Appium |
| ❌ 不支持原生应用 | 只支持 Web 应用 | 未来集成 Appium |
| ❌ 不支持文件上传 | 暂未实现 | 可手动扩展 |
| ❌ 不支持iframe切换 | 暂未实现 | 可手动扩展 |
| ⚠️ 浏览器版本依赖 | 需要匹配的 WebDriver | 使用 WebDriver Manager |
| ⚠️ 并发执行有限 | 受服务器资源限制 | 增加服务器配置 |

---

## 七、常见问题

### 7.1 如何选择测试对象？

**Q: 没有自己的网站怎么办？**

**A:** 使用推荐的公开测试网站（贴近业务场景）：
1. **OrangeHRM** - 企业HR管理系统，功能完整，适合业务测试
2. **Swag Labs** - 电商购物平台，完整购物流程
3. **Guru99 Bank** - 银行系统，金融业务场景
4. **ParaBank** - 银行系统演示，账户管理、转账等
5. **DemoQA** - 现代化UI，表单和交互测试

### 7.2 如何获取元素定位？

**Q: 不知道怎么写定位表达式？**

**A:** 使用浏览器开发者工具：

```
1. 打开网页（如百度首页）
2. 按 F12 打开开发者工具
3. 点击左上角的元素选择器图标（鼠标图标）
4. 点击要定位的元素（如搜索框）
5. 在 Elements 面板中会高亮显示该元素的 HTML 代码
6. 查看元素的属性：
   - 如果有 id="kw" → 使用 ID 定位，值为 "kw"
   - 如果有 class="s_ipt" → 使用 CLASS_NAME 定位，值为 "s_ipt"
   - 右键点击元素 → Copy → Copy selector → 得到 CSS 选择器
   - 右键点击元素 → Copy → Copy XPath → 得到 XPath
```

### 7.3 测试用例执行失败怎么办？

**Q: 为什么我的用例总是失败？**

**A:** 常见原因：

1. **元素定位失败**
   - 检查定位表达式是否正确
   - 检查页面是否完全加载（增加等待时间）
   - 使用浏览器开发者工具验证定位

2. **等待时间不足**
   - 增加强制等待时间
   - 页面加载慢时，等待时间可能需要 3-5 秒

3. **网络问题**
   - 检查测试网站是否可访问
   - 检查网络连接是否稳定

4. **浏览器版本不匹配**
   - 确保 ChromeDriver 版本与 Chrome 浏览器版本匹配
   - 使用 WebDriver Manager 自动管理

### 7.4 能否测试需要登录的页面？

**Q: 如何测试登录后的功能？**

**A:** 有两种方式：

**方式一：在用例中包含登录步骤**
```
步骤1: 打开登录页
步骤2: 输入用户名
步骤3: 输入密码
步骤4: 点击登录
步骤5-N: 执行后续测试
```

**方式二：使用 Cookie 跳过登录（高级）**
- 手动登录一次，保存 Cookie
- 在测试中直接设置 Cookie
- （需要自定义扩展实现）

### 7.5 如何调试测试用例？

**Q: 用例失败了，怎么知道哪里出错？**

**A:** 调试技巧：

1. **查看测试报告**
   - 查看失败步骤
   - 查看错误信息
   - 查看截图（如果配置了）

2. **增加截图**
   - 在关键步骤设置"是否截图"为 true
   - 在失败前的步骤截图，查看页面状态

3. **增加等待时间**
   - 在每个操作后增加 1-2 秒等待
   - 观察是否是时序问题

4. **本地手动执行**
   - 手动打开浏览器
   - 按照用例步骤手动操作
   - 验证定位表达式是否有效

### 7.6 为什么不支持移动端测试？

**Q: 能否测试 Android/iOS App？**

**A:** 当前不支持，原因：

1. **技术栈限制**
   - Selenium 只支持浏览器
   - 移动端需要 Appium

2. **架构设计**
   - 当前架构为 Web UI 自动化设计
   - 移动端需要重新设计

3. **未来规划**
   - 计划集成 Appium
   - 支持 Android/iOS 原生应用
   - 支持移动端浏览器（H5）

**临时解决方案：**
- 测试移动端 Web（H5）时，可以使用浏览器的移动端模式
- 在 Chrome 中按 F12 → 切换设备工具栏 → 选择手机模拟

### 7.7 如何测试复杂交互？

**Q: 如何测试拖拽、鼠标悬停等复杂操作？**

**A:** 使用对应的操作类型：

| 复杂交互 | 操作类型 | 配置 |
|---------|---------|------|
| 拖拽 | 鼠标拖拽元素到目标元素 | 需要配置源元素和目标元素 |
| 悬停 | 鼠标移入元素 | 定位要悬停的元素 |
| 双击 | 鼠标左键双击 | 定位要双击的元素 |
| 右键 | 鼠标右键点击 | 定位要右键点击的元素 |

**示例：测试拖拽功能**
```yaml
步骤1:
  操作类型: 鼠标拖拽元素到目标元素
  定位方式: ID
  定位表达式: draggable
  目标定位方式: ID
  目标定位表达式: droppable
  描述: 将元素拖拽到目标区域
```

### 7.8 如何显示浏览器窗口（观察自动化过程）？

**Q: 执行用例时看不到浏览器窗口，如何让浏览器显示出来？**

**A:** 系统默认会根据环境自动判断是否显示浏览器窗口。如果需要手动控制，可以通过以下方式：

#### 方式一：自动检测（推荐）

系统会自动检测 `DISPLAY` 环境变量：
- **有图形界面**：如果设置了 `DISPLAY` 环境变量，浏览器会自动显示窗口
- **无图形界面**：如果没有 `DISPLAY`，浏览器会在后台运行（headless 模式）

#### 方式二：手动配置环境变量

**在 WSL2 中配置 X11 转发（显示浏览器窗口）：**

1. **Windows 端安装 X11 服务器**
   - 下载并安装 [VcXsrv](https://sourceforge.net/projects/vcxsrv/) 或 [X410](https://x410.dev/)
   - 启动 X11 服务器，允许来自网络的连接

2. **WSL2 中设置 DISPLAY 环境变量**
   ```bash
   # 获取 Windows 主机 IP（WSL2 专用）
   export DISPLAY=$(cat /etc/resolv.conf | grep nameserver | awk '{print $2}'):0.0
   
   # 或者使用 localhost（如果配置了端口转发）
   export DISPLAY=localhost:0.0
   
   # 永久设置（添加到 ~/.bashrc）
   echo 'export DISPLAY=$(cat /etc/resolv.conf | grep nameserver | awk '{print $2}'):0.0' >> ~/.bashrc
   ```

3. **启动后端服务**
   ```bash
   # 确保 DISPLAY 已设置
   echo $DISPLAY
   
   # 启动服务
   cd backend/glen-engine
   mvn spring-boot:run
   ```

4. **执行测试用例**
   - 现在浏览器窗口会显示在 Windows 桌面上
   - 您可以实时观察自动化执行过程

**强制使用无头模式（不显示窗口）：**
```bash
# 设置环境变量强制使用无头模式
export UI_HEADLESS_MODE=true
```

**强制显示窗口（即使没有 DISPLAY）：**
```bash
# 设置环境变量强制显示窗口（需要图形界面支持）
export UI_HEADLESS_MODE=false
```

#### 方式三：在服务器环境中使用 VNC

如果后端服务运行在远程服务器上，可以使用 VNC：

1. **安装 VNC Server**
   ```bash
   sudo apt install tigervnc-standalone-server
   vncserver :1
   ```

2. **设置 DISPLAY**
   ```bash
   export DISPLAY=:1
   ```

3. **使用 VNC 客户端连接**
   - 使用 VNC Viewer 连接到服务器
   - 可以看到浏览器窗口

#### 注意事项

⚠️ **性能影响：**
- 显示浏览器窗口会消耗更多资源
- 在 CI/CD 环境中建议使用 headless 模式

⚠️ **WSL2 特殊说明：**
- WSL2 默认没有图形界面
- 需要配置 X11 转发才能显示窗口
- 如果不需要观察过程，使用 headless 模式性能更好

---

## 八、进阶技巧

### 8.1 步骤复用功能

**使用场景：**
登录步骤在多个用例中重复使用

**配置方法：**

```yaml
# 用例1：创建基础登录步骤
用例名称: 基础步骤库
步骤1:
  名称: 打开登录页
  步骤类型: LOCAL  # 本地步骤
  操作类型: 打开窗口
  值: https://example.com/login

步骤2:
  名称: 输入用户名
  步骤类型: LOCAL
  操作类型: 键盘输入
  定位方式: ID
  定位表达式: username
  值: testuser

# 用例2：引用登录步骤
用例名称: 查看个人信息
步骤1:
  名称: 执行登录
  步骤类型: REFER  # 引用步骤
  引用步骤ID: 1  # 引用用例1的步骤1

步骤2:
  名称: 点击个人中心
  步骤类型: LOCAL
  操作类型: 鼠标左键点击
  ...
```

详见：[UI步骤复用功能使用说明.md](./UI步骤复用功能使用说明.md)

### 8.2 定位表达式最佳实践

| 场景 | 推荐定位方式 | 示例 |
|------|------------|------|
| 有唯一 ID | ID | `#user-name` → `user-name` |
| 有唯一 Class | CSS_SELECTOR | `.btn-primary` |
| 复杂结构 | CSS_SELECTOR | `div.form > input[type='text']` |
| 动态 ID | XPATH | `//button[contains(text(),'登录')]` |
| 表单元素 | NAME | `username` |
| 链接文本 | LINK_TEXT | `点击登录` |

**避免使用：**
- ❌ 过长的 XPath：`/html/body/div[1]/div[2]/div[3]/form/input[1]`
- ❌ 索引定位：`//div[5]/span[2]`（页面改动易失效）
- ⚠️ 动态生成的 ID：`react-id-123456`

### 8.3 测试数据管理

**方法一：硬编码（适合简单场景）**
```yaml
步骤: 输入用户名
值: testuser
```

**方法二：使用环境变量（未来支持）**
```yaml
步骤: 输入用户名
值: ${TEST_USERNAME}
```

**方法三：数据驱动（未来支持）**
```yaml
用例: 登录测试
数据集:
  - { username: user1, password: pass1 }
  - { username: user2, password: pass2 }
```

---

## 九、总结

### 9.1 Glen UI 自动化适用场景

✅ **适合：**
- Web 应用功能测试
- 回归测试自动化
- 冒烟测试
- 端到端业务流程验证

❌ **不适合：**
- 移动端原生应用测试（暂不支持）
- 性能测试（应使用压测模块）
- 复杂的视觉测试
- 需要大量并发的测试

### 9.2 学习路径建议

```
第1周：熟悉基础操作
  ├─ 使用 OrangeHRM 或 Swag Labs 练习基础操作
  ├─ 掌握常用定位方式（ID、CSS_SELECTOR、NAME）
  └─ 理解测试报告

第2周：实战练习
  ├─ 测试完整业务流程（登录 → 操作 → 验证）
  ├─ 使用 OrangeHRM、Swag Labs、Guru99 Bank 练习
  └─ 学习断言的使用

第3周：高级功能
  ├─ 使用步骤复用功能
  ├─ 复杂交互（拖拽、悬停）
  └─ 测试用例优化

持续提升：
  ├─ 建立自己的步骤库
  ├─ 总结定位表达式技巧
  └─ 参与功能改进和扩展
```

### 9.3 参考资源

| 资源 | 链接 | 说明 |
|------|------|------|
| Selenium 官方文档 | https://www.selenium.dev/documentation/ | Selenium 完整文档 |
| 测试练习网站集合 | https://github.com/mdn/learning-area | 各种测试场景 |
| CSS选择器教程 | https://www.w3schools.com/cssref/css_selectors.php | 学习 CSS 选择器 |
| XPath教程 | https://www.w3schools.com/xml/xpath_intro.asp | 学习 XPath |

---

## 附录

### A. 常用测试网站清单

| 网站名称 | URL | 特点 |
|---------|-----|------|
| The Internet | https://the-internet.herokuapp.com/ | 自动化测试专用 |
| DemoQA | https://demoqa.com/ | 现代化UI |
| Sauce Demo | https://www.saucedemo.com/ | 完整电商流程 |
| LambdaTest Playground | https://www.lambdatest.com/selenium-playground/ | 各种UI元素 |
| Automation Practice | http://automationpractice.com/ | 电商网站 |
| ParaBank | https://parabank.parasoft.com/ | 银行系统 |

### B. 浏览器开发者工具快捷键

| 功能 | Windows/Linux | macOS |
|------|---------------|-------|
| 打开开发者工具 | F12 或 Ctrl+Shift+I | Cmd+Option+I |
| 元素选择器 | Ctrl+Shift+C | Cmd+Shift+C |
| 控制台 | Ctrl+Shift+J | Cmd+Option+J |

### C. 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v1.0 | 2026-01-21 | 初始版本，基础功能完成 |
| v1.1 | 待定 | 计划添加步骤复用功能 |
| v2.0 | 待定 | 计划集成 Appium，支持移动端 |

---

**文档作者：** Glen AutoTest Team  
**最后更新：** 2026-01-21  
**文档版本：** v1.0

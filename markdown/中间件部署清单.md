# 中间件部署清单

**部署日期**: 2026年1月11日  
**部署方式**: Docker容器化部署

---

## ✅ 已部署的中间件

### 1. MySQL 8.0 ✅

- **容器名称**: `dcloud-mysql`
- **端口映射**: `3306:3306`
- **数据目录**: `D:\docker-data\mysql\data`
- **配置文件**: `D:\docker-data\mysql\conf`
- **Root密码**: `yunautotest`
- **状态**: ✅ 运行中
- **数据库**: 
  - test_account (6个表)
  - test_api (5个表)
  - test_ui (5个表)
  - test_stress (3个表)
  - test_job (2个表)
  - test_dict (1个表，51条数据)
  - nacos_config (12个表)
  - test_engine (空数据库)

---

### 2. Redis 7.X ✅

- **容器名称**: `dcloud-redis`
- **端口映射**: `6379:6379`
- **数据目录**: `D:\docker-data\redis\data`
- **密码**: `123456`
- **版本**: `redis:7.0.8`
- **状态**: ✅ 运行中
- **测试连接**:
  ```bash
  docker exec -it dcloud-redis redis-cli -a 123456
  > PING
  > PONG
  ```

---

### 3. Nacos 2.X ✅

- **容器名称**: `dcloud-nacos`
- **端口映射**: 
  - `8848:8848` (HTTP API)
  - `9848:9848` (gRPC)
- **日志目录**: `D:\docker-data\nacos\logs`
- **版本**: `nacos/nacos-server:v2.2.3`
- **模式**: standalone (单机模式)
- **数据库**: nacos_config (连接到MySQL)
- **状态**: ✅ 运行中
- **访问控制台**:
  - URL: http://localhost:8848/nacos
  - 用户名: `nacos`
  - 密码: `nacos`
- **数据库配置**:
  - 主机: `host.docker.internal:3306`
  - 数据库: `nacos_config`
  - 用户: `root`
  - 密码: `yunautotest`

---

### 4. Zookeeper ✅

- **容器名称**: `dcloud-zookeeper`
- **端口映射**: `2181:2181`
- **版本**: `wurstmeister/zookeeper:latest`
- **状态**: ✅ 运行中
- **用途**: Kafka依赖服务

---

### 5. Kafka ✅

- **容器名称**: `dcloud-kafka`
- **端口映射**: `9092:9092`
- **版本**: `wurstmeister/kafka:latest`
- **Broker ID**: `1`
- **Zookeeper连接**: `host.docker.internal:2181`
- **监听地址**: `PLAINTEXT://127.0.0.1:9092`
- **环境变量**:
  - `KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://127.0.0.1:9092`
  - `KAFKA_LISTENERS=PLAINTEXT://0.0.0.0:9092`
- **状态**: ✅ 运行中
- **用途**: 消息队列，用于测试报告异步处理

---

### 6. MinIO 对象存储 ✅

- **容器名称**: `dcloud-minio`
- **端口映射**: 
  - `9000:9000` (API)
  - `9001:9001` (控制台)
- **数据目录**: `D:\docker-data\minio\data`
- **版本**: `minio/minio:latest`
- **Root用户**: `minio_root`
- **Root密码**: `minio_123456`
- **状态**: ✅ 运行中
- **访问控制台**:
  - URL: http://localhost:9001
  - 用户名: `minio_root`
  - 密码: `minio_123456`
- **需要创建Bucket**:
  1. 登录控制台 http://localhost:9001
  2. 点击 "Buckets" -> "Create Bucket"
  3. 输入名称: `bucket`
  4. 点击 "Create"

---

## 📋 容器管理命令

### 查看所有容器状态
```bash
docker ps --filter "name=dcloud-" --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
```

### 查看容器日志
```bash
# MySQL
docker logs -f dcloud-mysql

# Redis
docker logs -f dcloud-redis

# Nacos
docker logs -f dcloud-nacos

# Kafka
docker logs -f dcloud-kafka

# MinIO
docker logs -f dcloud-minio
```

### 重启容器
```bash
docker restart <容器名称>
```

### 停止容器
```bash
docker stop <容器名称>
```

### 启动容器
```bash
docker start <容器名称>
```

---

## 🔧 连接信息汇总

| 中间件 | 连接地址 | 用户名/密码 | 备注 |
|--------|---------|------------|------|
| MySQL | localhost:3306 | root / yunautotest | - |
| Redis | localhost:6379 | - / 123456 | - |
| Nacos | http://localhost:8848/nacos | nacos / nacos | 控制台 |
| Kafka | localhost:9092 | - | - |
| MinIO API | http://localhost:9000 | minio_root / minio_123456 | API端点 |
| MinIO 控制台 | http://localhost:9001 | minio_root / minio_123456 | Web控制台 |

---

## ✅ 部署检查清单

- [x] MySQL 8.0 已部署并导入所有SQL数据
- [x] Redis 7.X 已部署并测试连接
- [x] Nacos 2.X 已部署并连接到MySQL
- [x] Zookeeper 已部署并运行
- [x] Kafka 已部署并连接到Zookeeper
- [x] MinIO 已部署（需要在控制台创建bucket）

---

## 📝 下一步操作

1. **验证Nacos控制台**: 访问 http://localhost:8848/nacos 确认Nacos正常运行
   - 用户名: `nacos`
   - 密码: `nacos`
2. **创建MinIO Bucket**: 访问 http://localhost:9001 创建名为 `bucket` 的存储桶
   - 用户名: `minio_root`
   - 密码: `minio_123456`
3. **修改后端配置文件**: 更新application.properties中的连接信息
4. **启动后端微服务**: 按照依赖顺序启动各个服务
5. **启动前端**: 安装依赖并启动前端服务

---

**所有中间件部署完成！** 🎉

**部署进度**: ✅ 6/6 (100%)

# glen-autotest 项目启动指南

> **项目名称**: glen-autotest（自动化测试平台）  
> **更新日期**: 2026年1月11日  
> **版本**: v1.0

---

## 📋 目录

- [一、环境准备](#一环境准备)
- [二、Docker 中间件启动](#二docker-中间件启动)
- [三、数据库初始化](#三数据库初始化)
- [四、后端服务启动](#四后端服务启动)
- [五、前端服务启动](#五前端服务启动)
- [六、验证和测试](#六验证和测试)
- [七、常见问题](#七常见问题)
- [八、快速启动脚本](#八快速启动脚本)

---

## 一、环境准备

### 1.1 必需软件

| 软件 | 版本要求 | 下载地址 | 说明 |
|------|---------|---------|------|
| **JDK** | 17+ | https://www.oracle.com/java/technologies/downloads/#java17 | Java 开发环境 |
| **Maven** | 3.8+ | https://maven.apache.org/download.cgi | Java 构建工具 |
| **Node.js** | 18+ | https://nodejs.org/ | 前端运行环境 |
| **pnpm** | 8.8.0+ | `npm install -g pnpm@8.8.0` | 前端包管理器（推荐） |
| **Docker Desktop** | 最新版 | https://www.docker.com/products/docker-desktop | 容器运行环境 |
| **IntelliJ IDEA** | 2022+ | https://www.jetbrains.com/idea/ | Java IDE（推荐） |
| **Navicat / DBeaver** | - | - | 数据库管理工具（可选） |

### 1.2 环境验证

打开终端（PowerShell 或 CMD），执行以下命令验证环境：

```powershell
# 验证 JDK
java -version
# 应显示：openjdk version "17.x.x" 或 java version "17.x.x"

# 验证 Maven
mvn -version
# 应显示：Apache Maven 3.8.x

# 验证 Node.js
node -v
# 应显示：v18.x.x 或更高

# 验证 pnpm
pnpm -v
# 应显示：8.8.0 或更高

# 验证 Docker
docker -v
# 应显示：Docker version 24.x.x 或更高

# 验证 Docker 是否运行
docker ps
# 应正常执行，无错误
```

### 1.3 配置 Maven 镜像（可选）

如果 Maven 下载依赖较慢，可以配置阿里云镜像：

编辑 `%USERPROFILE%\.m2\settings.xml`（如不存在则创建）：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<settings>
    <mirrors>
        <mirror>
            <id>aliyunmaven</id>
            <mirrorOf>*</mirrorOf>
            <name>阿里云公共仓库</name>
            <url>https://maven.aliyun.com/repository/public</url>
        </mirror>
    </mirrors>
</settings>
```

---

## 二、Docker 中间件启动

### 2.1 启动 Docker Desktop

1. 打开 **Docker Desktop** 应用程序
2. 等待 Docker 启动完成（右下角图标变为绿色）
3. 验证 Docker 运行状态：
   ```powershell
   docker ps
   ```

### 2.2 启动 MySQL 8.0

**如果容器已存在，直接启动**：

```powershell
docker start dcloud-mysql
```

**如果容器不存在，创建并启动**：

```powershell
# 创建数据目录（Windows）
New-Item -ItemType Directory -Force -Path "D:\docker-data\mysql\data"
New-Item -ItemType Directory -Force -Path "D:\docker-data\mysql\conf"

# 启动 MySQL
docker run -d `
  --name dcloud-mysql `
  -p 3306:3306 `
  -e MYSQL_ROOT_PASSWORD=yunautotest `
  -v D:\docker-data\mysql\data:/var/lib/mysql `
  -v D:\docker-data\mysql\conf:/etc/mysql/conf.d `
  --restart=always `
  mysql:8.0

# 查看日志
docker logs -f dcloud-mysql
```

**验证 MySQL 启动**：

```powershell
# 等待 30 秒后测试连接
Test-NetConnection -ComputerName localhost -Port 3306
# 或使用 Navicat/DBeaver 连接
# 主机: localhost
# 端口: 3306
# 用户名: root
# 密码: yunautotest
```

### 2.3 启动 Redis 7.X

**如果容器已存在，直接启动**：

```powershell
docker start dcloud-redis
```

**如果容器不存在，创建并启动**：

```powershell
# 创建数据目录（Windows）
New-Item -ItemType Directory -Force -Path "D:\docker-data\redis\data"

# 启动 Redis
docker run -d `
  --name dcloud-redis `
  -p 6379:6379 `
  -v D:\docker-data\redis\data:/data `
  --restart=always `
  redis:7.0.8 `
  --requirepass 123456

# 查看日志
docker logs -f dcloud-redis
```

**验证 Redis 启动**：

```powershell
# 测试连接
docker exec -it dcloud-redis redis-cli -a 123456
# 执行 PING，应返回 PONG
# 执行 exit 退出
```

### 2.4 启动 Nacos 2.X

**重要**：Nacos 依赖 MySQL，必须先启动 MySQL。

**如果容器已存在，直接启动**：

```powershell
docker start dcloud-nacos
```

**如果容器不存在，创建并启动**：

```powershell
# 创建日志目录（Windows）
New-Item -ItemType Directory -Force -Path "D:\docker-data\nacos\logs"

# 启动 Nacos（Windows 使用 host.docker.internal 访问宿主机）
docker run -d `
  --name dcloud-nacos `
  -e MODE=standalone `
  -e JVM_XMS=256m `
  -e JVM_XMX=256m `
  -e SPRING_DATASOURCE_PLATFORM=mysql `
  -e MYSQL_SERVICE_HOST=host.docker.internal `
  -e MYSQL_SERVICE_PORT=3306 `
  -e MYSQL_SERVICE_USER=root `
  -e MYSQL_SERVICE_PASSWORD=yunautotest `
  -e MYSQL_SERVICE_DB_NAME=nacos_config `
  -e MYSQL_SERVICE_DB_PARAM='characterEncoding=utf8&connectTimeout=10000&socketTimeout=30000&autoReconnect=true&useSSL=false' `
  -p 8848:8848 `
  -p 9848:9848 `
  --restart=always `
  --privileged=true `
  -v D:\docker-data\nacos\logs:/home/nacos/logs `
  nacos/nacos-server:v2.2.3

# 查看日志（等待 30-60 秒）
docker logs -f dcloud-nacos
```

**验证 Nacos 启动**：

1. 等待 30-60 秒，让 Nacos 完全启动
2. 浏览器访问：http://localhost:8848/nacos
3. 登录信息：
   - 用户名: `nacos`
   - 密码: `nacos`
4. 登录后查看"服务管理" -> "服务列表"，应该为空（服务启动后会显示）

### 2.5 启动其他中间件（可选）

项目还使用以下中间件，根据实际需要启动：

> **💡 重要提示**：
> - **Kafka和MinIO不是每次都需要启动**，如果容器已经存在且设置了`--restart=always`，Docker会在系统启动时自动启动它们
> - **如果容器已存在但未运行**，只需执行 `docker start <容器名>` 启动即可
> - **如果容器不存在**，才需要执行下面的创建命令

#### 2.5.1 检查容器状态

```powershell
# 检查Kafka相关容器状态
docker ps -a --filter "name=dcloud-zookeeper"
docker ps -a --filter "name=dcloud-kafka"
docker ps -a --filter "name=dcloud-minio"

# 如果容器存在但未运行，启动它们
docker start dcloud-zookeeper
docker start dcloud-kafka
docker start dcloud-minio
```

#### 2.5.2 如果容器不存在，创建并启动

**Kafka**（消息队列）：
```powershell
# 1. 启动 Zookeeper（Kafka的依赖）
# 命令说明：
#   -d: 后台运行（detached mode）
#   --name dcloud-zookeeper: 容器名称
#   -p 2181:2181: 端口映射（主机端口:容器端口）
#   --restart=always: 容器自动重启策略（系统重启后自动启动）
#   wurstmeister/zookeeper:latest: 使用的镜像
docker run -d --name dcloud-zookeeper -p 2181:2181 --restart=always wurstmeister/zookeeper:latest

# 2. 启动 Kafka
# 命令说明：
#   -d: 后台运行
#   --name dcloud-kafka: 容器名称
#   -p 9092:9092: 端口映射（Kafka服务端口）
#   -e KAFKA_BROKER_ID=1: 环境变量，设置Kafka Broker ID
#   -e KAFKA_CFG_ZOOKEEPER_CONNECT=host.docker.internal:2181: 连接Zookeeper的地址
#   -e KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://127.0.0.1:9092: 对外暴露的地址
#   -e KAFKA_CFG_LISTENERS=PLAINTEXT://0.0.0.0:9092: 监听地址
#   --restart=always: 自动重启
docker run -d `
  --name dcloud-kafka `
  -p 9092:9092 `
  -e KAFKA_BROKER_ID=1 `
  -e KAFKA_CFG_ZOOKEEPER_CONNECT=host.docker.internal:2181 `
  -e KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://127.0.0.1:9092 `
  -e KAFKA_CFG_LISTENERS=PLAINTEXT://0.0.0.0:9092 `
  --restart=always `
  wurstmeister/kafka:latest
```

**MinIO**（对象存储，用于存储截图、文件等）：
```powershell
# 1. 创建数据目录（如果不存在）
New-Item -ItemType Directory -Force -Path "D:\docker-data\minio\data"

# 2. 启动 MinIO
# 命令说明：
#   -d: 后台运行
#   --name dcloud-minio: 容器名称
#   -p 9000:9000: API端口
#   -p 9001:9001: 控制台端口
#   -e MINIO_ROOT_USER=minio_root: 管理员用户名
#   -e MINIO_ROOT_PASSWORD=minio_123456: 管理员密码
#   -v D:\docker-data\minio\data:/data: 数据卷映射（持久化存储）
#   --restart=always: 自动重启
#   server /data --console-address ":9001": 启动命令
docker run -d `
  --name dcloud-minio `
  -p 9000:9000 `
  -p 9001:9001 `
  -e MINIO_ROOT_USER=minio_root `
  -e MINIO_ROOT_PASSWORD=minio_123456 `
  -v D:\docker-data\minio\data:/data `
  --restart=always `
  minio/minio:latest `
  server /data --console-address ":9001"
```

**访问 MinIO 控制台**：http://localhost:9001
- 用户名: `minio_root`
- 密码: `minio_123456`
- **重要**：首次使用需要创建名为 `bucket` 的存储桶（Bucket）

#### 2.5.3 如果容器名称冲突（已存在同名容器）

如果遇到错误：`Conflict. The container name "/dcloud-xxx" is already in use`

**解决方案1：启动已有容器（推荐）**
```powershell
# 启动已存在的容器
docker start dcloud-zookeeper
docker start dcloud-kafka
docker start dcloud-minio
```

**解决方案2：删除并重建容器**
```powershell
# 停止并删除旧容器
docker stop dcloud-zookeeper dcloud-kafka dcloud-minio
docker rm dcloud-zookeeper dcloud-kafka dcloud-minio

# 然后重新执行创建命令（见2.5.2）
```

**解决方案3：使用不同名称**
```powershell
# 如果不想删除旧容器，可以使用不同的名称
docker run -d --name glen-zookeeper -p 2181:2181 --restart=always wurstmeister/zookeeper:latest
docker run -d --name glen-kafka -p 9092:9092 ...
```

#### 2.5.4 中间件使用说明

| 中间件 | 用途 | 是否必需 | 说明 |
|--------|------|---------|------|
| **Kafka** | 消息队列 | ⚠️ 部分功能需要 | 用于分布式压测、异步任务等 |
| **MinIO** | 对象存储 | ⚠️ 部分功能需要 | 用于存储UI测试截图、上传的文件等 |

**何时需要启动**：
- ✅ **Kafka**：执行分布式压测时需要
- ✅ **MinIO**：执行UI自动化测试（需要截图）或上传文件时需要
- ❌ **如果只做简单的接口测试**，可以不启动这两个中间件

### 2.6 查看所有容器状态

```powershell
# 查看所有容器（包括停止的）
docker ps -a

# 查看运行中的容器
docker ps

# 查看特定容器状态
docker ps --filter "name=dcloud-"

# 查看容器日志
docker logs -f dcloud-mysql
docker logs -f dcloud-redis
docker logs -f dcloud-nacos
```

### 2.7 中间件连接信息汇总

| 中间件 | 连接地址 | 用户名/密码 | 备注 |
|--------|---------|------------|------|
| **MySQL** | localhost:3306 | root / yunautotest | 数据库 |
| **Redis** | localhost:6379 | - / 123456 | 缓存 |
| **Nacos** | http://localhost:8848/nacos | nacos / nacos | 服务注册中心 |
| **Kafka** | localhost:9092 | - | 消息队列（可选） |
| **MinIO API** | http://localhost:9000 | minio_root / minio_123456 | 对象存储（可选） |
| **MinIO 控制台** | http://localhost:9001 | minio_root / minio_123456 | Web 控制台（可选） |

---

## 三、数据库初始化

### 3.1 创建数据库

使用 Navicat 或命令行工具连接到 MySQL，执行以下 SQL：

```sql
-- 创建数据库（如果不存在）
CREATE DATABASE IF NOT EXISTS glen_account DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_api DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_ui DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_stress DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_engine DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_job DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS glen_dict DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE DATABASE IF NOT EXISTS nacos_config DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

### 3.2 导入 SQL 文件

**方式一：使用 Navicat（推荐）**

1. 打开 Navicat，连接到 MySQL
2. 依次选择数据库，右键 -> "运行 SQL 文件"
3. 选择对应的 SQL 文件并执行

**方式二：使用命令行**

```powershell
# 进入项目根目录
cd E:\resume\yunautotest

# 导入 account 数据库
docker exec -i dcloud-mysql mysql -uroot -pyunautotest glen_account < account_sql\account.sql

# 导入其他数据库（根据实际 SQL 文件路径）
docker exec -i dcloud-mysql mysql -uroot -pyunautotest glen_api < dcloud_api_sql\dcloud_api.sql
docker exec -i dcloud-mysql mysql -uroot -pyunautotest glen_ui < dcloud_ui_sql\dcloud_ui_sql.sql
docker exec -i dcloud-mysql mysql -uroot -pyunautotest glen_stress < dcloud_stress_sql\dcloud_stress.sql
docker exec -i dcloud-mysql mysql -uroot -pyunautotest glen_job < job_sql\job.sql
docker exec -i dcloud-mysql mysql -uroot -pyunautotest glen_dict < sys_dict\sys_dict.sql
```

### 3.3 验证数据库

使用 Navicat 查看数据库，确认表和数据已正确导入。

---

## 四、后端服务启动

### 4.1 打开项目

1. 打开 **IntelliJ IDEA**
2. 选择 `File` -> `Open`
3. 选择项目目录：`E:\resume\yunautotest\backend\glen-autotest`
4. 选择 `pom.xml` 文件，点击 `OK`
5. 等待 IDEA 索引和 Maven 依赖下载完成

### 4.2 Maven 编译

在 IDEA 中：

1. 右键点击根目录的 `pom.xml`
2. 选择 `Maven` -> `Reload Project`
3. 等待依赖下载完成
4. 选择 `Build` -> `Rebuild Project`（可选，用于清理重新编译）

或使用命令行：

```powershell
cd E:\resume\yunautotest\backend\glen-autotest
mvn clean compile -DskipTests
```

### 4.3 服务启动顺序

**重要**：必须按照以下顺序启动服务！

1. **glen-common**（公共模块，无需启动）
2. **glen-account**（账号服务，端口 8081）
3. **glen-data**（数据服务，端口 8082）
4. **glen-engine**（引擎服务，端口 8083）
5. **glen-gateway**（网关服务，端口 8000）⭐最后启动

### 4.4 在 IDEA 中启动服务（推荐）

**方式一：使用 Run Configuration（最推荐）**

1. 找到各个服务的启动类文件（具体路径如下）：
   - **glen-account**: 
     - 文件路径：`backend/glen-autotest/glen-account/src/main/java/com/glen/autotest/AccountApplication.java`
     - 完整类名：`com.glen.autotest.AccountApplication`
   
   - **glen-data**: 
     - 文件路径：`backend/glen-autotest/glen-data/src/main/java/com/glen/autotest/DataApplication.java`
     - 完整类名：`com.glen.autotest.DataApplication`
   
   - **glen-engine**: 
     - 文件路径：`backend/glen-autotest/glen-engine/src/main/java/com/glen/autotest/EngineApplication.java`
     - 完整类名：`com.glen.autotest.EngineApplication`
   
   - **glen-gateway**: 
     - 文件路径：`backend/glen-autotest/glen-gateway/src/main/java/com/glen/autotest/GatewayApplication.java`
     - 完整类名：`com.glen.autotest.GatewayApplication`

2. 在 IDEA 中打开对应的启动类文件

3. 右键点击启动类文件中的类名（如 `AccountApplication`），选择 `Run 'AccountApplication'`

4. 依次启动所有服务，每个服务使用独立的运行配置

**方式二：使用 Maven 插件**

1. 在 IDEA 右侧打开 `Maven` 工具窗口（如果没有，点击 `View` -> `Tool Windows` -> `Maven`）

2. 展开对应的模块（如 `glen-account`）-> `Plugins` -> `spring-boot`

3. 双击 `spring-boot:run` 启动服务

4. 重复上述步骤启动其他服务

**提示**：
- 使用 IDEA 启动服务后，**不需要**再去命令行启动，两种方式选择其一即可
- IDEA 启动的优势：可以在同一个 IDE 中查看所有服务的日志，方便调试
- 每个服务启动后，会在 IDEA 底部的 `Run` 窗口中显示日志

### 4.5 使用命令行启动服务（可选）

**说明**：这是另一种启动方式，与 IDEA 启动方式二选一即可。如果已经在 IDEA 中启动了服务，就不需要再使用命令行启动。

**Windows PowerShell**（需要打开多个终端窗口）：

```powershell
# 终端1：启动 account-service
cd E:\resume\yunautotest\backend\glen-autotest\glen-account
mvn spring-boot:run

# 终端2：启动 data-service
cd E:\resume\yunautotest\backend\glen-autotest\glen-data
mvn spring-boot:run

# 终端3：启动 engine-service
cd E:\resume\yunautotest\backend\glen-autotest\glen-engine
mvn spring-boot:run

# 终端4：启动 gateway-service（最后启动）
cd E:\resume\yunautotest\backend\glen-autotest\glen-gateway
mvn spring-boot:run
```

**使用场景**：
- 不想打开 IDEA 时使用
- 需要在服务器上部署时使用
- 需要自动化脚本启动时使用

### 4.6 验证后端服务启动

**检查 Nacos 服务注册**：

1. 访问：http://localhost:8848/nacos
2. 登录（用户名: `nacos`，密码: `nacos`）
3. 点击 "服务管理" -> "服务列表"
4. 应该看到以下服务（状态为"健康"）：
   - `account-service`（1个实例）
   - `data-service`（1个实例）
   - `engine-service`（1个实例）
   - `gateway-service`（1个实例）

**检查服务健康状态**（使用 PowerShell）：

```powershell
# 检查 account-service
Invoke-WebRequest -Uri "http://localhost:8081/actuator/health" -UseBasicParsing

# 检查 data-service
Invoke-WebRequest -Uri "http://localhost:8082/actuator/health" -UseBasicParsing

# 检查 engine-service
Invoke-WebRequest -Uri "http://localhost:8083/actuator/health" -UseBasicParsing

# 检查 gateway-service
Invoke-WebRequest -Uri "http://localhost:8000/actuator/health" -UseBasicParsing
```

**查看服务日志**：

在 IDEA 的 Run 窗口中查看各服务的启动日志，确认没有错误。

---

## 五、前端服务启动

### 5.1 进入前端目录

```powershell
cd E:\resume\yunautotest\frontend\glen-autotest-frontend
```

### 5.2 安装依赖

**首次启动需要安装依赖**：

```powershell
# 使用 pnpm（推荐）
pnpm install

# 或使用 npm
npm install
```

**如果依赖已安装，可以跳过此步骤。**

### 5.3 启动开发服务器

```powershell
# 使用 pnpm
pnpm dev

# 或使用 npm
npm run dev
```

### 5.4 验证前端启动

启动成功后，终端会显示：

```
  VITE v5.0.10  ready in 1234 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to help
```

**访问前端页面**：

- 登录页面：http://localhost:5173/#/login
- 首页：http://localhost:5173/

**测试账号**：

- 账号：`13800138000`
- 密码：`admin123`

---

## 六、验证和测试

### 6.1 完整启动检查清单

✅ **Docker 容器**：
- [ ] MySQL 运行中（端口 3306）
- [ ] Redis 运行中（端口 6379）
- [ ] Nacos 运行中（端口 8848，可访问控制台）

✅ **数据库**：
- [ ] 所有数据库已创建（glen_account、glen_api 等）
- [ ] SQL 文件已导入
- [ ] 测试账号存在

✅ **后端服务**：
- [ ] account-service 启动成功（端口 8081）
- [ ] data-service 启动成功（端口 8082）
- [ ] engine-service 启动成功（端口 8083）
- [ ] gateway-service 启动成功（端口 8000）
- [ ] 所有服务已注册到 Nacos

✅ **前端服务**：
- [ ] 前端服务启动成功（端口 5173）
- [ ] 可以访问登录页面
- [ ] 可以使用测试账号登录

### 6.2 功能测试

1. **登录测试**：
   - 访问：http://localhost:5173/#/login
   - 使用测试账号登录（13800138000 / admin123）

2. **项目管理测试**：
   - 登录后，创建项目
   - 验证项目列表显示正常

3. **接口测试**：
   - 访问接口自动化测试模块
   - 创建模块、接口、用例
   - 执行测试用例

4. **查看测试报告**：
   - 查看测试报告列表
   - 查看报告详情

---

## 七、常见问题

### 7.1 Docker 相关问题

**Q: Docker Desktop 无法启动？**

A: 
1. 检查是否已启用 WSL2
2. 检查系统是否满足 Docker Desktop 要求
3. 重启计算机
4. 重新安装 Docker Desktop

**Q: 容器启动失败？**

A: 
1. 查看容器日志：`docker logs <容器名>`
2. 检查端口是否被占用：`netstat -ano | findstr :3306`
3. 检查数据目录权限
4. 删除容器重新创建：`docker rm -f <容器名>`

**Q: Nacos 无法访问？**

A: 
1. 等待 30-60 秒，Nacos 启动需要时间
2. 查看日志：`docker logs -f dcloud-nacos`
3. 检查 MySQL 是否正常运行
4. 检查数据库 `nacos_config` 是否已创建
5. 确认端口 8848 未被占用

### 7.2 数据库相关问题

**Q: 数据库连接失败？**

A: 
1. 检查 MySQL 容器是否运行：`docker ps | findstr mysql`
2. 检查数据库是否已创建
3. 检查用户名、密码是否正确（root / yunautotest）
4. 检查数据库名称是否正确（glen_account 等）

**Q: SQL 导入失败？**

A: 
1. 检查 SQL 文件编码（应为 UTF-8）
2. 检查 SQL 文件路径是否正确
3. 使用 Navicat 手动导入 SQL 文件
4. 检查 SQL 语法是否正确

### 7.3 后端服务相关问题

**Q: 服务启动失败？**

A: 
1. 检查 JDK 版本（应为 17+）
2. 检查 Maven 依赖是否下载完成
3. 查看启动日志中的错误信息
4. 检查配置文件中的数据库连接信息
5. 检查 Nacos 是否正常运行

**Q: 服务注册到 Nacos 失败？**

A: 
1. 检查 Nacos 是否正常运行
2. 检查 `application.properties` 中的 Nacos 地址：`localhost:8848`
3. 检查防火墙是否开放 8848 端口
4. 查看服务启动日志中的注册错误信息

**Q: 服务端口被占用？**

A: 
1. 查找占用端口的进程：
   ```powershell
   netstat -ano | findstr :8081
   ```
2. 关闭占用端口的进程，或修改服务端口

### 7.4 前端相关问题

**Q: 前端无法启动？**

A: 
1. 检查 Node.js 版本（应为 18+）
2. 删除 `node_modules` 和 `pnpm-lock.yaml`，重新安装依赖
3. 检查端口 5173 是否被占用
4. 查看错误信息，解决具体问题

**Q: 前端无法连接后端？**

A: 
1. 检查后端服务是否都已启动
2. 检查 Gateway 服务是否运行在端口 8000
3. 检查前端配置文件中的 API 地址：`http://localhost:8000`
4. 检查浏览器控制台的网络错误信息

**Q: 登录失败？**

A: 
1. 检查后端服务是否正常运行
2. 检查测试账号是否存在
3. 检查账号权限配置
4. 查看后端日志中的错误信息

---

## 八、快速启动脚本

### 8.1 一键启动 Docker 容器（PowerShell）

创建文件 `start-docker.ps1`：

```powershell
# 启动所有 Docker 容器
Write-Host "正在启动 Docker 容器..." -ForegroundColor Green

# 启动 MySQL
Write-Host "启动 MySQL..." -ForegroundColor Yellow
docker start dcloud-mysql 2>$null
if ($LASTEXITCODE -ne 0) {
    Write-Host "MySQL 容器不存在，请先创建容器" -ForegroundColor Red
} else {
    Write-Host "MySQL 启动成功" -ForegroundColor Green
}

# 启动 Redis
Write-Host "启动 Redis..." -ForegroundColor Yellow
docker start dcloud-redis 2>$null
if ($LASTEXITCODE -ne 0) {
    Write-Host "Redis 容器不存在，请先创建容器" -ForegroundColor Red
} else {
    Write-Host "Redis 启动成功" -ForegroundColor Green
}

# 启动 Nacos
Write-Host "启动 Nacos..." -ForegroundColor Yellow
docker start dcloud-nacos 2>$null
if ($LASTEXITCODE -ne 0) {
    Write-Host "Nacos 容器不存在，请先创建容器" -ForegroundColor Red
} else {
    Write-Host "Nacos 启动成功（等待 30-60 秒完全启动）" -ForegroundColor Green
}

# 查看容器状态
Write-Host "`n容器状态：" -ForegroundColor Cyan
docker ps --filter "name=dcloud-"
```

### 8.2 一键停止 Docker 容器（PowerShell）

创建文件 `stop-docker.ps1`：

```powershell
# 停止所有 Docker 容器
Write-Host "正在停止 Docker 容器..." -ForegroundColor Yellow

docker stop dcloud-mysql dcloud-redis dcloud-nacos 2>$null

Write-Host "所有容器已停止" -ForegroundColor Green
```

### 8.3 检查服务状态脚本（PowerShell）

创建文件 `check-status.ps1`：

```powershell
# 检查所有服务状态
Write-Host "=== 服务状态检查 ===" -ForegroundColor Cyan

# 检查 Docker 容器
Write-Host "`n1. Docker 容器状态：" -ForegroundColor Yellow
docker ps --filter "name=dcloud-" --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 检查后端服务
Write-Host "`n2. 后端服务状态：" -ForegroundColor Yellow
$services = @(
    @{Name="account-service"; Port=8081},
    @{Name="data-service"; Port=8082},
    @{Name="engine-service"; Port=8083},
    @{Name="gateway-service"; Port=8000}
)

foreach ($service in $services) {
    try {
        $response = Invoke-WebRequest -Uri "http://localhost:$($service.Port)/actuator/health" -UseBasicParsing -TimeoutSec 2 -ErrorAction Stop
        Write-Host "  ✅ $($service.Name) (端口 $($service.Port)) - 运行中" -ForegroundColor Green
    } catch {
        Write-Host "  ❌ $($service.Name) (端口 $($service.Port)) - 未运行" -ForegroundColor Red
    }
}

# 检查前端服务
Write-Host "`n3. 前端服务状态：" -ForegroundColor Yellow
try {
    $response = Invoke-WebRequest -Uri "http://localhost:5173" -UseBasicParsing -TimeoutSec 2 -ErrorAction Stop
    Write-Host "  ✅ 前端服务 (端口 5173) - 运行中" -ForegroundColor Green
} catch {
    Write-Host "  ❌ 前端服务 (端口 5173) - 未运行" -ForegroundColor Red
}

# 检查 Nacos
Write-Host "`n4. Nacos 控制台：" -ForegroundColor Yellow
try {
    $response = Invoke-WebRequest -Uri "http://localhost:8848/nacos" -UseBasicParsing -TimeoutSec 2 -ErrorAction Stop
    Write-Host "  ✅ Nacos (端口 8848) - 可访问" -ForegroundColor Green
    Write-Host "  访问地址: http://localhost:8848/nacos" -ForegroundColor Gray
    Write-Host "  用户名/密码: nacos / nacos" -ForegroundColor Gray
} catch {
    Write-Host "  ❌ Nacos (端口 8848) - 不可访问" -ForegroundColor Red
}
```

---

## 九、启动顺序总结

### 完整启动流程：

1. **启动 Docker Desktop** ✅
2. **启动 Docker 容器**（MySQL、Redis、Nacos）✅
3. **验证数据库**（确认数据已导入）✅
4. **启动后端服务**（account → data → engine → gateway）✅
5. **验证后端服务**（检查 Nacos 服务列表）✅
6. **启动前端服务** ✅
7. **访问前端页面** ✅

### 日常启动（服务已配置好）：

1. 启动 Docker Desktop
2. 运行 `start-docker.ps1` 启动容器
3. 在 IDEA 中启动所有后端服务
4. 运行 `pnpm dev` 启动前端服务
5. 访问 http://localhost:5173

---

## 十、相关文档

- [项目重命名指南.md](./项目重命名指南.md) - 项目重命名详细步骤
- [中间件部署清单.md](./中间件部署清单.md) - Docker 中间件部署信息
- [Docker.md](./Docker.md) - Docker 学习文档
- [账密.md](./账密.md) - 账号密码信息

---

**最后更新**: 2026年1月11日  
**文档版本**: v1.0  
**维护者**: glen-autotest 项目组


## 整理启动
请补充简洁版本的启动指南：

1、启动Docker Desktop

2、启动中间件
1. docker start dcloud-mysql
2. docker start dcloud-redis
3. docker start dcloud-nacos

4. docker ps -a --filter "name=dcloud-zookeeper"
5. docker ps -a --filter "name=dcloud-kafka"
6. docker ps -a --filter "name=dcloud-minio"

7. docker start dcloud-zookeeper
8. docker start dcloud-kafka
9. docker start dcloud-minio

3、IDEA启动服务
   - **glen-account**: 
     - 文件路径：`backend/glen-autotest/glen-account/src/main/java/com/glen/autotest/AccountApplication.java`
   
   - **glen-data**: 
     - 文件路径：`backend/glen-autotest/glen-data/src/main/java/com/glen/autotest/DataApplication.java`
   
   - **glen-engine**: 
     - 文件路径：`backend/glen-autotest/glen-engine/src/main/java/com/glen/autotest/EngineApplication.java`

   - **glen-gateway**: 
     - 文件路径：`backend/glen-autotest/glen-gateway/src/main/java/com/glen/autotest/GatewayApplication.java`
       
4、登录Nacos、MinlO

5、启动前端
PS E:\resume\yunautotest\frontend\glen-autotest-frontend> pnpm dev




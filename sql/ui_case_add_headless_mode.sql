-- ============================================
-- Glen 自动化云测平台 - UI 用例表增加显示窗口控制字段
-- ============================================
-- 功能：允许用户在前端控制是否显示浏览器窗口
-- 作者：Glen AutoTest Team
-- 日期：2026-01-21
-- ============================================

USE glen_ui;

-- 增加 headless_mode 字段
ALTER TABLE ui_case 
ADD COLUMN headless_mode TINYINT(1) DEFAULT 0 
COMMENT '无头模式 0-显示窗口 1-无头模式';

-- 验证字段是否添加成功
DESCRIBE ui_case;

-- 查看现有数据（应该都默认为 0）
SELECT id, name, headless_mode FROM ui_case LIMIT 5;

-- ============================================
-- 使用说明：
-- headless_mode = 0：显示浏览器窗口（默认，便于调试和观察）
-- headless_mode = 1：无头模式（后台运行，速度更快）
-- ============================================
